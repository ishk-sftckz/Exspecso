---
phase: 01-initialize-canonical-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SETUP-03, ART-03, ART-08]
nyquist_compliant: true
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "vitest@4.1.11 is not installed until a human verifies its provenance, repository, maintainers, publication history, and lifecycle scripts."
    - "@inquirer/prompts@8.6.0 is not installed until a human verifies its provenance, repository, maintainers, publication history, and lifecycle scripts."
    - "The public stable-ID family used by ART-03 is explicitly selected and recorded before source code encodes it."
  artifacts:
    - path: ".planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md"
      provides: "Recorded package approvals and stable-ID vocabulary decision"
  key_links:
    - from: ".planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md"
      to: "package.json"
      via: "Plan 02 precondition blocks dependency installation without both approvals"
    - from: ".planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md"
      to: "src/artifacts/schema.ts"
      via: "Plan 04 implements the selected ID vocabulary"
---

## Phase Goal

**As an** Exspecso user, **I want to** initialize a project with only my selected runtime adapters and durable canonical foundations, **so that** project truth remains portable and inspectable.

<objective>
Authorize the two research-flagged packages and freeze the stable-ID vocabulary before any dependency install or public artifact contract is encoded.

Purpose: Close the package-supply-chain and public-contract gates without mutating the greenfield repository.
Output: Human approvals for `vitest@4.1.11` and `@inquirer/prompts@8.6.0`, plus one recorded stable-ID vocabulary choice for ART-03.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify vitest@4.1.11 package legitimacy</name>
  <read_first>
    - .planning/phases/01-initialize-canonical-projects/01-RESEARCH.md — Package Legitimacy Audit and Validation Architecture
    - .planning/phases/01-initialize-canonical-projects/01-VALIDATION.md — manual package-verification contract
  </read_first>
  <files>None — read-only approval checkpoint</files>
  <what-built>Research selected the exact `vitest@4.1.11` test dependency but marked it [SUS], so installation is blocked pending legitimacy evidence.</what-built>
  <how-to-verify>Review the automated `npm view` metadata, open the linked official repository and npm package page, verify provenance/maintainers/publication history/lifecycle scripts, then explicitly approve or reject the exact version.</how-to-verify>
  <action>Inspect `vitest@4.1.11` on npm and its linked official repository. Confirm provenance, repository ownership, publication history, maintainers, and absence or acceptability of lifecycle scripts. Record the evidence and approval in the plan summary. Do not install or change `package.json` during this checkpoint.</action>
  <verify>
    <automated>npm view vitest@4.1.11 name version repository maintainers dist.integrity scripts --json</automated>
    <human-check>Compare the registry metadata with the official Vitest project and approve or reject the exact version.</human-check>
  </verify>
  <acceptance_criteria>Human explicitly approves `vitest@4.1.11`, or execution stops without installing it.</acceptance_criteria>
  <done>The exact Vitest version has a recorded legitimacy disposition before Plan 02.</done>
  <resume-signal>Type "approved vitest@4.1.11" or describe the legitimacy concern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify @inquirer/prompts@8.6.0 package legitimacy</name>
  <read_first>
    - .planning/phases/01-initialize-canonical-projects/01-RESEARCH.md — Package Legitimacy Audit and TTY prompt recommendation
    - .planning/phases/01-initialize-canonical-projects/01-VALIDATION.md — manual package-verification contract
  </read_first>
  <files>None — read-only approval checkpoint</files>
  <what-built>Research selected the exact `@inquirer/prompts@8.6.0` TTY dependency but marked it [SUS], so installation is blocked pending legitimacy evidence.</what-built>
  <how-to-verify>Review the automated `npm view` metadata, open the linked official repository and npm package page, verify provenance/maintainers/publication history/lifecycle scripts, then explicitly approve or reject the exact version.</how-to-verify>
  <action>Inspect `@inquirer/prompts@8.6.0` on npm and its linked official repository. Confirm provenance, repository ownership, publication history, maintainers, and absence or acceptability of lifecycle scripts. Record the evidence and approval in the plan summary. Do not install or change `package.json` during this checkpoint.</action>
  <verify>
    <automated>npm view @inquirer/prompts@8.6.0 name version repository maintainers dist.integrity scripts --json</automated>
    <human-check>Compare the registry metadata with the official Inquirer project and approve or reject the exact version.</human-check>
  </verify>
  <acceptance_criteria>Human explicitly approves `@inquirer/prompts@8.6.0`, or execution stops without installing it.</acceptance_criteria>
  <done>The exact prompt-package version has a recorded legitimacy disposition before Plan 02.</done>
  <resume-signal>Type "approved @inquirer/prompts@8.6.0" or describe the legitimacy concern.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Freeze the public stable-ID vocabulary</name>
  <read_first>
    - .planning/REQUIREMENTS.md — ART-03, ART-04, ART-06, and ART-09
    - .planning/phases/01-initialize-canonical-projects/01-RESEARCH.md — Open Question 2
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-01 through D-04
  </read_first>
  <files>None — decision is recorded in 01-01-SUMMARY.md</files>
  <action>Choose the stable public identifier families that the artifact parser and resolver will recognize. This decision supplements D-01 through D-04 without changing their locked config contents or the opaque project identity. Record the exact literals so Plan 04 can implement them without interpretation.</action>
  <decision>Which stable-ID vocabulary should Exspecso encode for Roadmap, Phase, Spec, Requirement, Acceptance Criterion, Plan, Task, Decision, and review finding?</decision>
  <context>This is a one-way public on-disk contract: changing issued identifiers after projects exist would break references or require migrations.</context>
  <options>
    <option id="option-a">
      <name>Concise canonical families</name>
      <pros>Uses `ROADMAP`, `PHASE-NNN`, `SPEC-NNN`, `REQ-NNN`, `AC-NNN`, `PLAN-NNN`, `TASK-NNN`, `DEC-NNN`, and `FINDING-NNN`; aligns with existing requirement language and keeps references compact.</pros>
      <cons>Abbreviated `REQ`, `AC`, and `DEC` require documentation.</cons>
    </option>
    <option id="option-b">
      <name>Expanded canonical families</name>
      <pros>Uses longer descriptive prefixes for requirement, acceptance criterion, and decision identifiers.</pros>
      <cons>Creates longer section anchors and departs from the repository's existing `PHASE-NNN`, `SPEC-NNN`, `TASK-NNN`, and `DEC-NNN` terminology.</cons>
    </option>
  </options>
  <verify>
    <automated>node -e "const required=['roadmap','phase','spec','requirement','acceptance-criterion','plan','task','decision','review-finding']; if(required.length!==9) process.exit(1)"</automated>
    <human-check>Confirm one option and the exact nine literals; no source implementation begins before the choice is recorded.</human-check>
  </verify>
  <acceptance_criteria>Exactly one vocabulary is approved and 01-01-SUMMARY.md records all nine identifier forms.</acceptance_criteria>
  <done>ART-03 has an explicit, implementation-ready ID vocabulary.</done>
  <resume-signal>Select `option-a` (recommended) or `option-b` and list any adjusted literal.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry metadata → maintainer approval | Untrusted package publication metadata is reviewed before it can authorize installation. |
| Human decision → public artifact contract | A recorded choice becomes an on-disk identifier contract consumed by later phases. |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | `vitest` and `@inquirer/prompts` installs | high | mitigate | Blocking human checkpoints validate exact versions, provenance, repositories, maintainers, publication history, integrity, and lifecycle scripts before Plan 02 may install. |
| T-01-01 | Spoofing | npm package identity | high | mitigate | Compare registry metadata to the official Vitest and Inquirer repositories; reject any mismatch. |
| T-01-02 | Tampering | stable-ID choice | medium | mitigate | Record the exact nine literals in the immutable plan summary and make Plan 04 consume that record. |
| T-01-03 | Repudiation | package approval | low | accept | The execution summary records the human resume signal and inspected metadata; local Git history is sufficient for this phase. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Decision record | Package legitimacy approvals | Exact approvals for `vitest@4.1.11` and `@inquirer/prompts@8.6.0` in `01-01-SUMMARY.md` |
| Decision record | Stable-ID vocabulary | Nine exact identifier literals consumed by `src/artifacts/schema.ts` and `src/artifacts/resolve.ts` |
| Path | `.planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md` | Executor-created checkpoint evidence and decisions |

<verification>
Both `npm view` commands return metadata for the exact versions, both human approvals are explicit, and the selected ID vocabulary contains one unambiguous literal for every ART-03 entity kind. No package or source file is created by this plan.
</verification>

<success_criteria>
- Both [SUS] packages have explicit human legitimacy dispositions before installation.
- The stable-ID vocabulary is recorded before any parser, schema, or resolver encodes it.
- High-severity supply-chain threats remain blocking until mitigated.
</success_criteria>

<output>
Create `.planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md` when done.
</output>
