---
phase: 01-initialize-canonical-projects
plan: 11
type: execute
wave: 11
depends_on: [01-10]
files_modified: [native/support-matrix.json, native/build.mjs, .github/workflows/containment.yml, scripts/containment-evidence.mjs, tests/unit/containment-evidence.test.ts]
autonomous: true
requirements: [SETUP-01, SETUP-02, ART-07]
gap_closure: true
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every required OS/CPU/libc row runs the real promotion tracer natively before namespace expansion begins; unavailable or skipped rows block this plan."
    - "Machine-readable evidence names exact Node/OS/filesystem/toolchain and provider hashes; one local pass cannot represent other targets."
    - "CI permission, resource cost, and Node compatibility decisions remain the explicit 01-09 choices."
  artifacts:
    - {path: native/support-matrix.json, provides: "Approved exact target rows and tested-version policy"}
    - {path: .github/workflows/containment.yml, provides: "Native target builds and tracer evidence"}
    - {path: scripts/containment-evidence.mjs, provides: "Required-row validation and evidence aggregation"}
  key_links:
    - {from: .github/workflows/containment.yml, to: native/support-matrix.json, via: "Declared required rows generate jobs", pattern: support-matrix}
    - {from: scripts/containment-evidence.mjs, to: tests/unit/containment-evidence.test.ts, via: "Missing/stale/emulated evidence rejection", pattern: evidence}
---

<objective>
Prove the proposed mechanism on all approved native environments before expanding the repair. Preserve D-19's measured evidence boundary and the existing phase goal; the historical MVP goal-format warning remains unchanged.
Purpose: Early Windows, macOS, glibc and musl feasibility must be real rather than inferred.
Output: Exact machine matrix, native build/test jobs, and a rejecting evidence gate.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-09-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-10-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run the one-path native tracer on the approved target matrix</name>
  <precondition>Read-only runner inventory confirms the exact native runners and approved access in 01-09; absence stops without purchasing resources or dispatching elsewhere.</precondition>
  <read_first>01-09-SUMMARY.md approval ledger; native/build.mjs; tests/integration/init-codex-tracer.test.ts; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md ENV rows.</read_first>
  <files>native/support-matrix.json, native/build.mjs, .github/workflows/containment.yml, scripts/containment-evidence.mjs, tests/unit/containment-evidence.test.ts</files>
  <behavior>Matrix validation rejects absent CPU/libc rows, unpinned images, emulation labeled native, unknown Node policy, and missing exact filesystem observations.</behavior>
  <action>First test matrix validation. Materialize only 01-09's approved ledger, including minimum claimed versions and actually tested versions as separate fields. Use immutable toolchain/image references where available and record observed versions when hosted images cannot be immutable. Build native release/test variants on each native row, run the installed promotion tracer, and upload JSON/log artifacts with source commit, target tuple, Node/NAPI, compiler, OS/kernel, filesystem, libc, binary SHA-256, and evidence mode. Musl jobs use approved Node provenance and actual musl linkage, not a glibc binary or CPU emulator. Keep npm dependency lock unchanged. Jobs are split into bounded test shards; no continue-on-error or missing-row success. Infrastructure writes/dispatch require the recorded authorization; workflow creation alone is not platform proof.</action>
  <verify><automated>npm test -- --run tests/unit/containment-evidence.test.ts</automated></verify>
  <external-evidence gate="blocking">Download actual native runner artifacts and run the tracer aggregate; record their CI run identity, exact environment, provider hash, reached tracer IDs and aggregate result in the summary. Local parser unit tests and workflow creation cannot satisfy this gate.</external-evidence>
  <acceptance_criteria>All eight proposed target families remain represented unless an explicit amended user decision exists. CI invokes the actual installed native tracer, and records exact measured environment data.</acceptance_criteria>
  <done>The portable tracer has an executable native matrix rather than success claims based on local tests.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make incomplete platform evidence block expansion</name>
  <read_first>scripts/containment-evidence.mjs; tests/unit/containment-evidence.test.ts; native/support-matrix.json; actual tracer artifacts from Task 1.</read_first>
  <files>scripts/containment-evidence.mjs, tests/unit/containment-evidence.test.ts, .github/workflows/containment.yml</files>
  <behavior>Missing, failed, skipped, cancelled, stale-commit, wrong-hash, duplicate/conflicting and emulation-only results all reject the aggregate; a complete exact matrix passes.</behavior>
  <action>Add an aggregate command with stage tracer and evidence-directory arguments. Verify required test IDs and evidence mode rather than trusting a job's exit code. Bind every record to the same source commit and approved matrix revision; differing per-platform binaries are expected but their hashes must match their build records. Run the aggregate over actual downloaded artifacts. If one required row is unavailable or the Windows relative replacement gates fail, return blocked and preserve the failure; do not continue to 01-12, drop the row, or substitute pathname calls. Store results in the execution summary and CI artifacts, not user canonical state.</action>
  <verify><automated>node scripts/containment-evidence.mjs --stage tracer --evidence-dir .artifacts/containment</automated></verify>
  <acceptance_criteria>Actual required native tracer rows pass; synthetic gate fixtures alone cannot complete this task. Failures remain actionable blockers.</acceptance_criteria>
  <done>All-platform tracer feasibility is proven to the chosen evidence standard before the remaining migration.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Build runners supply binaries and evidence consumed as release inputs.
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Spoofing | matrix evidence | high | mitigate | Native host observations, commit/hash binding, and rejecting aggregate fixtures |
| T-11-02 | Tampering | CI configuration | high | mitigate | Exact approved rows, pinned actions/images where supported, protected artifact provenance |
| T-11-SC | Tampering | compiler and dependency inputs | high | mitigate | Approved pinned toolchains and existing audited lock; new package requires legitimacy gate |
</threat_model>
## Artifacts this phase produces
native/support-matrix.json approved row schema; native/build.mjs target/variant output; containment.yml jobs; containment-evidence.mjs validation/aggregation; corresponding unit tests. Generated binaries/logs are CI artifacts, not canonical project data.
<verification>Focused gate unit tests, actual native tracer jobs, and tracer aggregate must all pass. Each shard reports within 60 seconds or is split without omitting cases.</verification>
<success_criteria>No required row is missing and all native tracer evidence matches the approved contract. This is not yet full containment verification.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-11-SUMMARY.md with actual target evidence and any infrastructure blockers.</output>
