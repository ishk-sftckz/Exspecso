---
phase: 01-initialize-canonical-projects
plan: 10
task: 3
status: paused-for-platform-access-clarification
last_updated: 2026-08-28
---

# Resume Plan 01-10 Task 3

Both the native design contract and the POSIX tracer were explicitly approved by the user (`approve`). Do not ask for either approval again.

Tasks 1 and 2 are verified. Task 2 RED `240e636`, GREEN `0093733`; final CI run 33144287790 / snapshot 48aeed78bfb8ea10cc130d9a15e230197d80e837 passed Windows provider 11/11, macOS focused 21/21 and full regression 77/77. All 43 source/config hashes match. Read `01-10-WINDOWS-REVIEW.md` and saved evidence. The documented Windows image refresh in SUPPORT preserves the OS/compiler/SDK pins and records the inspected UBR.

Continue Task 3: actual installed CLI evidence on Windows plus full historical vulnerable-CLI leaf/parent/ancestor RED reproductions. Native Windows test controller and cross-platform package/test harness adaptation are still pending. Do not expand into Plan 11 or claim complete Windows initialization/recovery from provider-only tests. Preserve the halted partial SUMMARY (2/3 tasks) until this plan actually completes. Phase 1 remains 9/18 complete.


## Latest interruption — Trusted Access banner

The user showed an OpenAI cybersecurity/Trusted Access banner and asked why it appeared after identity verification. New security test runs were paused while explaining the platform message. The assistant cannot inspect account provisioning or confirm the exact trigger, and must not bypass platform safeguards. Both project approvals remain valid; do not request them again. The task objective remains Phase 1 gaps, not a new scope decision.

Task 3 changes are present but uncommitted in `tests/helpers/containment-fixture.ts`, `tests/integration/init-codex-tracer.test.ts`, `tests/fixtures/contained-vulnerable/`, and `.github/workflows/containment-windows-parity.yml`. This is partial test infrastructure, not a verified task or release. No Windows native controller has been added.

- Historical source ledger reconstructs all 18 original source files from bbe2e5db32e06bc66f7c9e1a87252270f174d9a3, preserving original transaction/run-init snapshots and checking unchanged files. An isolated historical installed package receives a fixed IPC barrier immediately before copyFile, after validation/mkdir. The controller must observe process `exit`; waiting for `close` after IPC disconnection hung and was corrected. Early timeout runs are not RED evidence.
- Local development historical grid now reproduces all three external sentinel overwrites. CI run 33144791900 / snapshot 8ec34a671091d340d351257cb2706cf20b9e2d63 also reproduces the three historical races on approved macOS and passes current focused 21/21 plus full regression 77/77. The filtered historical invocation has eight deliberately unselected tests; the full green invocation has no skips.
- Windows in that run passes provider 11/11, but the historical CLI's initial normal invocation exits 1 before the race. Its stderr has not yet been diagnosed; these failures are NOT historical vulnerability reproductions. Consequently the Windows installed green step did not run. Preserve this distinction and do not mark Task 3 done.
- Windows checkout now sets core.autocrlf=false before checkout so the historical source hashes refer to original LF bytes. The earlier Task 2 snapshot ledger matched Git blob inputs; the Windows build provenance separately records hashes of its CRLF checkout. Task 3 must retain exact working-tree input hashes too.
- Saved reports/logs/snapshot ledgers and local RED evidence: `01-10-EVIDENCE/task-3-in-progress/`. Both submitted CI runs are complete; no test job remains running.
- Another user task committed `91eddbb` (managed skill frontmatter/ownership) during this work. Do not revert it or upload unrelated changes. It changes an original source used by the historical ledger; freeze that old file in the historical fixture before resuming historical tests rather than weakening the hash check. Rebase/reverify current green expectations against this user's change.
- Remote testing branch is `codex/containment-windows-parity-20260828`, last snapshot 8ec34a671091d340d351257cb2706cf20b9e2d63. Default remote branch was not pushed. Temporary snapshot script/evidence cache is `/var/folders/gb/rm0q4rln62b29shqvhmg3d5c0000gn/T/exspecso-windows-ci-1xkveirh/`.
