---
phase: 01-initialize-canonical-projects
plan: 21
type: execute
wave: 20
depends_on: [01-18]
files_modified:
  - src/filesystem/journal.ts
  - src/filesystem/transaction.ts
  - src/filesystem/recovery.ts
  - src/filesystem/contained-fs.ts
  - tests/integration/transaction-recovery.test.ts
  - tests/unit/root-scoped-fs.test.ts
autonomous: true
requirements: [ART-07]
gap_closure: true
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[D-18; D-19; D-21; ART-07] A failure before or during the first durable preparation-journal write cannot leave nonempty staging evidence that makes every later recovery permanently ambiguous; recovery removes only positively identified pre-promotion debris and preserves unknown evidence."
    - "[D-21; ART-07] FileCapability.read() terminates with a stable containment error when a descriptor reaches zero bytes before its observed length is consumed, and it revalidates descriptor identity and size before returning bytes."
  artifacts:
    - path: src/filesystem/journal.ts
      provides: "Schema-2 preparation state that proves promotion has not begun"
    - path: src/filesystem/transaction.ts
      provides: "Write-ahead preparation ordering before staged bytes, backups, or destination-parent mutation"
    - path: src/filesystem/recovery.ts
      provides: "Conservative cleanup for identified pre-journal and preparing evidence"
    - path: tests/integration/transaction-recovery.test.ts
      provides: "Deterministic pre-journal and target-parent failure reproductions plus ambiguity-preservation regression"
    - path: src/filesystem/contained-fs.ts
      provides: "Bounded descriptor reads with zero-progress rejection and final descriptor revalidation"
    - path: tests/unit/root-scoped-fs.test.ts
      provides: "Deterministic truncation-during-read and final-stat mismatch coverage"
  key_links:
    - from: src/filesystem/transaction.ts
      to: src/filesystem/recovery.ts
      via: "schema-2 preparing journal and strictly identified stage inventory"
      pattern: "preparing"
    - from: src/filesystem/contained-fs.ts
      to: tests/unit/root-scoped-fs.test.ts
      via: "zero-progress and final fstat identity/size assertions"
      pattern: "fstatSync"
---

<objective>
Close the two remaining Phase 1 verifier blockers in the pure TypeScript/Node filesystem path.

Purpose: Make transaction preparation recoverable before promotion can occur, and make repository reads fail closed instead of hanging when a concurrent truncation invalidates the initially observed descriptor state.
Output: A write-ahead preparation state with conservative recovery, a bounded FileCapability read loop with final descriptor revalidation, and deterministic regressions for both failures.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md
@.planning/phases/01-initialize-canonical-projects/01-REVIEW.md
@.planning/phases/01-initialize-canonical-projects/01-17-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-18-SUMMARY.md

This is a bounded gap closure after completed 01-18. Reuse the active journal/transaction/recovery state machine and the Node filesystem capability from 01-17. Preserve D-21's host-permission/sandbox boundary and do not restore native containment or expand compatibility, packaging, publication, documentation, registry, or TTY scope.

<interfaces>
Existing contracts to extend without replacing:

- journal.ts: TransactionState, TransactionJournal, parseTransactionJournal()
- transaction.ts: CommitTransactionOptions, commitTransaction(), readBoundTransactionJournal(), removeRecoveredTransactionBound()
- recovery.ts: RecoveryResult, recoverInterruptedTransaction()
- contained-fs.ts: FileCapability.read(), DirectoryCapability.openFile(), BoundReader.read()

Existing test seams and helpers to reuse:

- CommitTransactionOptions.onBeforeStaging
- createGitFixture(), snapshotTree(), stagingRelativePath, readTransactionJournal()
- Vitest module spies/mocks for deterministic node:fs read/stat behavior
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make preparation write-ahead and recover every identified pre-promotion failure</name>
  <read_first>.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md gaps[0]; .planning/phases/01-initialize-canonical-projects/01-REVIEW.md CR-01; .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md D-18, D-19, and D-21; src/filesystem/journal.ts; src/filesystem/transaction.ts; src/filesystem/recovery.ts; src/filesystem/ownership.ts; tests/integration/transaction-recovery.test.ts; .planning/phases/01-initialize-canonical-projects/01-18-SUMMARY.md.</read_first>
  <files>src/filesystem/journal.ts, src/filesystem/transaction.ts, src/filesystem/recovery.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>
    - RED reproduction A: after safe-path validation, change a planned preimage through the existing onBeforeStaging seam so commit fails after creating its transaction directory but before staging bytes; restore the preimage and require recovery to remove the empty identified stage and return restored-prior rather than permanent ambiguity.
    - RED reproduction B: after safe-path validation, substitute a planned destination ancestor so parent acquisition fails after the current implementation has staged bytes; remove the substitution, preserve an external sentinel, and require recovery to return restored-prior with the prior repository bytes and no staging debris.
    - Preparation recovery: a valid preparing journal proves no promotion started; recovery may remove only its declared partial files/backups and empty parents after validating journal identity, repository fingerprint, state invariants, known inventory, and unchanged canonical preimages.
    - Ambiguity preservation: an unknown entry, symlink, malformed preparation record, invalid transaction-directory name, or promotion evidence that conflicts with preparation state remains present and returns ambiguous.
  </behavior>
  <action>Write both failing reproductions and the negative ambiguity case first, run the focused file, and record the expected RED output before production edits. Per D-18, D-19, and D-21, extend the existing schema-2 TransactionState with a preparing state whose parser requires inFlight null, no completed promotions, and completedStep -1. In commitTransaction(), create the transaction directory, compute preimages and the complete intended entry/order metadata using reads only, and durably publish the preparing journal before creating any staged file, backup, or destination parent. Then write and sync the declared staged/backup entries, validate their inventory and hashes, advance the same journal to prepared, and continue through the existing promotion/recovery machinery. If entry construction fails before the preparing journal exists, leave only an empty transaction directory. In recovery, accept a missing journal only for a UUID-named transaction directory whose contents prove it is either empty or contains solely the implementation's one regular atomic-journal temporary; remove exactly that evidence. For a parsed preparing journal, require a strict subset of journal-declared staging paths, reject every unlisted or unsafe entry, confirm canonical targets still match recorded preimages, remove only the declared operational evidence and transaction-created empty parents, and never rewrite a canonical target because promotion was impossible. Keep current prepared/promoting/restoring/cleaning and legacy-journal behavior intact. Do not introduce another recovery framework, schema family, dependency, native path, or cleanup outside the held operational capabilities.</action>
  <verify>
    <automated>npm test -- --run tests/integration/transaction-recovery.test.ts &amp;&amp; npm run build &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>The two old-code reproductions are recorded red before implementation; afterward empty pre-journal and valid partial-preparing states recover to the unchanged prior set, target-parent failure no longer leaves permanent ambiguity once its external cause is removed, unknown evidence remains byte-for-byte present and ambiguous, and the focused plus full suites pass.</acceptance_criteria>
  <done>The transaction has durable recovery authority before any stage-content or destination-side mutation, and every accepted cleanup is bounded to positively identified pre-promotion evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound FileCapability reads and revalidate the descriptor before return</name>
  <read_first>.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md gaps[1]; .planning/phases/01-initialize-canonical-projects/01-REVIEW.md CR-02; .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md D-21; src/filesystem/contained-fs.ts FileCapability and NodeBoundReader; tests/unit/root-scoped-fs.test.ts.</read_first>
  <files>src/filesystem/contained-fs.ts, tests/unit/root-scoped-fs.test.ts</files>
  <behavior>
    - RED reproduction: deterministically truncate an opened regular file after the initial descriptor stat but before the next read, make a second zero-progress iteration fail the test instead of hanging the test runner, and require one stable changed-during-read containment error.
    - Final-stat reproduction: after returning the initially requested bytes from the mocked read boundary, make the final descriptor stat report changed size or identity and require the same fail-closed result.
    - Stable read: an unchanged descriptor returns exactly the initially observed bytes and preserves current regular-file and closed-capability behavior.
  </behavior>
  <action>Write the deterministic Vitest reproductions first and run the focused file red before changing FileCapability. Use a hoisted node:fs spy/mock or an equivalently deterministic local seam: delegate real descriptor operations, trigger the truncation only after the initial fstat, and guard an unexpected second zero-return so the current infinite-loop defect is demonstrated without wedging Vitest. In FileCapability.read(), retain the initial bigint descriptor stat including device, inode, and size; on every readSync call, reject zero progress while offset is below the observed length with a stable EXSPECSO_CONTAINMENT_CHANGED error. After consuming the expected length, fstat the same descriptor again and reject any device, inode, size, or regular-file mismatch before returning the buffer. Ensure containment errors are propagated without double-wrapping by nodeFailure. Preserve the current synchronous capability API, root scoping, component checks, symlink checks, and D-21 host-sandbox limitation; do not add polling, retries, native code, or platform-specific branches.</action>
  <verify>
    <automated>npm test -- --run tests/unit/root-scoped-fs.test.ts &amp;&amp; npm run build &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>The old read loop is demonstrated red without hanging; afterward a premature zero-byte read and final descriptor identity/size change each throw EXSPECSO_CONTAINMENT_CHANGED, unchanged reads still return exact bytes, and the focused plus full suites pass.</acceptance_criteria>
  <done>Concurrent truncation cannot trap the initializer in an unbounded read loop or return bytes after the observed descriptor identity/size changes.</done>
</task>

</tasks>

## Artifacts this phase produces

- New production files: none.
- New exported production functions or classes: none required. The existing `TransactionState` union gains the `preparing` state; existing transaction, recovery, and capability APIs remain the integration surface.
- New test files: none.
- New tests in existing files: pre-journal empty-stage recovery, target-parent failure with preparation recovery, unknown preparation evidence preservation, zero-progress truncation rejection, and final descriptor identity/size rejection.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Transaction plan → repository mutation | Planned hashes and paths cross into repository-local staging, journal, destination-parent creation, and promotion. |
| Open descriptor → returned canonical bytes | A concurrently changed repository file can invalidate the size and identity observed before FileCapability.read() completes. |

## STRIDE Threat Register — ASVS Level 1; block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-01 | Tampering | transaction.ts / journal.ts / recovery.ts | high | mitigate | Publish a schema-validated preparing record before stage-content or destination mutation; clean only exact known preparation inventory and preserve all unknown evidence. |
| T-21-02 | Denial of Service | contained-fs.ts FileCapability.read() | high | mitigate | Reject zero-progress reads before the observed length and revalidate descriptor device, inode, kind, and size before returning. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Minimal durable canonical foundations survive interruption and repository-file change without hidden ambiguous state or a hung initializer | 01-21 Tasks 1-2 | COVERED | Closes only the two blockers in the independent verifier report. |
| REQ | ART-07 | Preserve the previous valid artifact set when an atomic workflow write fails or is interrupted | 01-21 Tasks 1-2 | COVERED | Transaction recovery and its protected reads must both terminate conservatively. |
| REQ | SETUP-01–08, ART-01–06, ART-08–09 | Remaining Phase 1 requirements | completed 01-01 through 01-18 | COVERED | Regression suite is rerun; 01-21 does not re-own or expand these behaviors. |
| RESEARCH | — | Plan-before-write, repository-local staging, durable journal, validated recovery, and focused fault tests | 01-21 Task 1 | COVERED | Reuses the established TypeScript state machine and capability boundaries. |
| RESEARCH | — | Root-scoped Node filesystem reads fail closed on changed repository state | 01-21 Task 2 | COVERED | Reuses FileCapability and Node built-ins only. |
| RESEARCH | — | Native containment and exhaustive certification | excluded by D-21 | EXCLUDED | Historical/deferred material, outside the shipped architecture and this gap closure. |
| CONTEXT | D-18 | Remove only clearly identified Exspecso debris; ambiguity and external change fail closed | 01-21 Task 1 | COVERED | Preparation cleanup is inventory- and state-bounded. |
| CONTEXT | D-19 | Deterministic process-failure evidence without power-loss or universal-filesystem claims | 01-21 Task 1 | COVERED | Tests reproduce reachable preparation failures without broadening the evidence claim. |
| CONTEXT | D-21 | Pure TypeScript/Node package, deterministic checks, conservative recovery, host sandbox boundary | 01-21 Tasks 1-2 | COVERED | No native or platform-specific implementation is introduced. |
| CONTEXT | D-01–17, D-20, D-22 | Other locked Phase 1 decisions | completed 01-01 through 01-18 | COVERED | Full regression protects them; no task changes their owned surfaces. |
| CONTEXT | Deferred native hardening | Optional native/kernel assistance | none | EXCLUDED | Explicitly deferred in 01-CONTEXT.md. |
| VERIFICATION | deferred registry publication | Registry-installed npx availability | Phase 6 | EXCLUDED | Explicitly deferred; no package/publication work in 01-21. |
| VERIFICATION | human TTY item | Real interactive rendering/cancellation | none | EXCLUDED | Explicitly excluded from this two-gap closure. |

<verification>
Run each RED reproduction before its production edit and retain the failing output in the task commit/summary. After each repair, run its exact focused Vitest file, `npm run build`, and the complete active suite with `npm test -- --run`. The final summary must record both RED results and both focused/build/full green command results; it must not edit the independent verification report or claim Phase 1 complete.
</verification>

<success_criteria>
- The transaction writes durable preparation authority before any staged bytes, backups, destination parents, or promotion can exist.
- Recovery cleans only strictly identified empty/atomic-journal/preparing evidence when promotion was impossible and preserves every unknown or conflicting entry as ambiguous.
- FileCapability.read() rejects premature zero progress and final descriptor identity, kind, or size changes without looping.
- Both focused suites, the TypeScript build, and the full active suite pass with no work outside the six declared files.
</success_criteria>

<output>
Create .planning/phases/01-initialize-canonical-projects/01-21-SUMMARY.md only after both tasks pass, recording the RED reproductions, write-ahead ordering, bounded recovery cases, descriptor-read guards, focused/build/full results, and the unchanged D-21 security boundary.
</output>
