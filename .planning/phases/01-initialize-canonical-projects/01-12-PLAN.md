---
phase: 01-initialize-canonical-projects
plan: 12
type: execute
wave: 12
depends_on: [01-11]
files_modified: [src/filesystem/contained-fs.ts, src/filesystem/git-root.ts, src/artifacts/resolve.ts, src/artifacts/validate.ts, src/init/plan.ts, src/init/run-init.ts, src/filesystem/safe-path.ts, tests/unit/artifacts.test.ts, tests/integration/validation-errors.test.ts]
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-06, SETUP-07, ART-01, ART-03, ART-04, ART-05, ART-06, ART-08, ART-09]
gap_closure: true
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Artifact validation, resolution and init preflight read through the opened repository capability; substitution cannot supply attacker-selected external bytes as write authority."
    - "Malformed declaration aggregation, duplicate resolution, exact D-20 IDs, and additive adapter preflight retain their existing assertions."
    - "Provider failure occurs before ownership or staging mutation, while empty transaction plans remain true no-ops."
  artifacts:
    - {path: src/filesystem/contained-fs.ts, provides: "Bound reader facade and owned-root lifetime"}
    - {path: src/artifacts/resolve.ts, provides: "Bound enumeration and lossless declaration diagnostics"}
    - {path: src/init/run-init.ts, provides: "Read-only provider preflight before mutation"}
  key_links:
    - {from: src/artifacts/validate.ts, to: src/artifacts/resolve.ts, via: "Shared bound reader and full scan diagnostics", pattern: scanArtifacts}
    - {from: src/init/plan.ts, to: src/filesystem/contained-fs.ts, via: "Bound preimage reads", pattern: ContainedFilesystem}
---
<objective>
Make the real initializer's reads safe without changing artifact meaning, runtime selection, or no-write diagnostics. Preserve D-01 through D-04, D-09 through D-17, and D-20. Historical MVP goal framing remains unchanged.
Purpose: Safe mutation cannot rely on an unsafe artifact/preimage read.
Output: One capability-rooted read path shared by artifact resolution, validation, and planning.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-11-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve and validate artifacts through bound reads</name>
  <read_first>src/artifacts/resolve.ts ArtifactScanResult and artifactFiles; src/artifacts/validate.ts validateProject; src/filesystem/contained-fs.ts; tests/unit/artifacts.test.ts; 01-07-SUMMARY.md.</read_first>
  <files>src/filesystem/contained-fs.ts, src/filesystem/git-root.ts, src/artifacts/resolve.ts, src/artifacts/validate.ts, tests/unit/artifacts.test.ts</files>
  <behavior>Leaf/directory substitutions during scan are rejected rather than followed; scanner preserves every independent invalid id/parent diagnostic; missing lazy artifacts remain valid; malformed/unreadable canonical input is never silently omitted.</behavior>
  <action>Introduce a small bound reader interface for enumeration, regular-file read, and no-follow metadata, backed solely by ContainedFilesystem. Resolve the initial Git-root object once; verify its Git marker through that capability and bind subsequent reads to it. Initial discovery remains an observation under the approved trusted-root assumption, not an atomic pathname guarantee. Let standalone scan/resolve/validate callers acquire and close a read capability when none is supplied; nested init callers reuse theirs. Keep ignored-directory behavior, normalized relative artifact locations, ordering, duplicate handling, raw declaration diagnostics, and D-20 parsing unchanged. Separate an absent path from unreadable/unsafe data; validation must not swallow a containment failure as an empty project. Reading never creates a lock, temp, or staging directory. Add RD-01/02/03 tests before the read migration.</action>
  <verify><automated>npm test -- --run tests/unit/artifacts.test.ts</automated></verify>
  <acceptance_criteria>Bound scan errors remain explicit; all old valid/malformed/duplicate/rename assertions pass. Reads introduce no canonical field or artifact.</acceptance_criteria>
  <done>A user receives the same complete artifact diagnostics without following substituted filesystem names.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind initializer preflight and stale-preimage checks to the same root</name>
  <read_first>src/init/run-init.ts runInit and validateStaged; src/init/plan.ts buildInitPlan/validateInitPlan; src/filesystem/safe-path.ts; tests/integration/validation-errors.test.ts; 01-CONTAINMENT-SUPPORT.md trusted mutation boundary.</read_first>
  <files>src/init/run-init.ts, src/init/plan.ts, src/filesystem/safe-path.ts, src/filesystem/contained-fs.ts, tests/integration/validation-errors.test.ts</files>
  <behavior>Missing/bad provider and read-only environment failures happen before lock/stage writes; invalid JSON blocks with exact tree equality; stale observed preimages block the complete change set; empty direct transaction remains no-op.</behavior>
  <action>Use the fixed in-package manifest, loader and dist/native host artifact already created and tested in 01-10; do not introduce packaging or an external loader in this plan. Load and validate the fixed included provider before any mutation and open the discovered root capability. Pass the same reader through preliminary validation, recovery-evidence inspection, current config/adapter reads, plan validation, and staged-config validation. Keep target fields for diagnostics but derive authority only from validated relative components. Preserve all D-12 conflicts and D-15 diagnostics before mutation. Remove read fallbacks that turn unsafe/unreadable content into absence. Close owned handles on all exits; caller-owned roots remain alive through subsequent ownership/transaction operations. Add RD-04 and provider preflight regressions using actual provider failure, not invalid CLI arguments. Capability support checks are read-only; do not test writability by creating probe files while claiming zero writes. Do not change public flags, selected-agent behavior, project identity, or the no-op shortcut.</action>
  <verify><automated>npm test -- --run tests/integration/validation-errors.test.ts tests/integration/init-rerun.test.ts tests/integration/minimal-artifacts.test.ts</automated></verify>
  <acceptance_criteria>Observed drift, invalid artifacts and provider failure cannot reach ownership/staging; successful init preserves all approved configuration/adapter invariants. The accepted concurrent-regular-file limitation remains explicit.</acceptance_criteria>
  <done>Init derives its write plan only from validated object-bound reads and preserves the no-write error contract.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
Repository names and artifact bytes enter validation and write authorization.
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Information disclosure | artifact reader | high | mitigate | No-follow capability enumeration/read; external sentinel reads are not used as authority |
| T-12-02 | Tampering | preflight | high | mitigate | Shared root, explicit unsafe-read errors, retained aggregate diagnostics and preimage checks |
| T-12-03 | Denial of service | handle lifecycle | medium | mitigate | Owned/caller-owned close semantics and failure-path tests |
</threat_model>
## Artifacts this phase produces
BoundReader/ContainedFilesystem read methods; optional reader parameters on scanArtifacts/validateProject/buildInitPlan/validateInitPlan; root-bound init lifetime; RD test cases in existing artifact and validation suites. No new persisted user state.
<verification>Run each focused command, npm run build, and the existing full regression suite. This plan improves reads; later write/cleanup migration remains required.</verification>
<success_criteria>All read authorities are object-bound and earlier artifact, selection and preflight semantics remain intact.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-12-SUMMARY.md with red/green read-race and no-write evidence.</output>

## Scope boundary

Task 1 owns the shared read facade plus artifact scanning/validation. Task 2 only wires that existing facade through init preflight and lifetime handling. Native provider implementation and loader/manifest packaging are predecessor outputs, not new work here; runtime selection, schemas and transaction-write changes remain outside this plan.
