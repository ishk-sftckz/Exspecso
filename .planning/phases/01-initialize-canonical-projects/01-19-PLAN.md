---
phase: 01-initialize-canonical-projects
plan: 19
type: execute
wave: 13
depends_on: [01-12]
files_modified:
  - native/support-matrix.json
  - src/filesystem/support-matrix.ts
  - src/filesystem/contained-fs.ts
  - native/build.mjs
  - tests/unit/containment-support.test.ts
  - package.json
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-06, SETUP-07, ART-01, ART-02, ART-07, ART-08, ART-09]
gap_closure: true
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The current macOS 26.5.1 build 25F80 arm64 local-APFS machine is a declared supported environment row, builds the direct Node-API 8 provider with its installed Command Line Tools, and loads that provider under Node 25.2.1."
    - "native/support-matrix.json is the primary support representation: loader and builder resolve rows from data, while unlisted or ambiguous OS/CPU/libc/filesystem/Node tuples fail before opening ownership or staging state."
    - "The eight previously approved rows and nine previously named Node lanes remain intact; the matrix contains at least nine rows and ten named lanes, including exact Node 25.2.1 support and no general odd/future-major allowance."
    - "Every declared support-row/manifest/runtime tuple is accepted by the pure matcher on its matching observation, and mutations that make the tuple undeclared are rejected."
  artifacts:
    - path: "native/support-matrix.json"
      provides: "Versioned platform rows, structured build policy, Node ranges/exceptions, and ten named test lanes"
    - path: "src/filesystem/support-matrix.ts"
      provides: "Parsed support contracts and pure row/Node matching"
      exports: ["SupportMatrix", "SupportRow", "RuntimeObservation", "loadSupportMatrix", "resolveSupportRow", "isSupportedNodeVersion"]
    - path: "src/filesystem/contained-fs.ts"
      provides: "Manifest selection through the matched support-row identity"
    - path: "native/build.mjs"
      provides: "Row-selected native build with exact toolchain provenance"
    - path: "tests/unit/containment-support.test.ts"
      provides: "All-declared-accepted and all-undeclared-rejected invariants"
  key_links:
    - from: "src/filesystem/contained-fs.ts"
      to: "native/support-matrix.json"
      via: "loadSupportMatrix then resolveSupportRow before manifest selection"
      pattern: "resolveSupportRow"
    - from: "native/build.mjs"
      to: "native/support-matrix.json"
      via: "--row selects the only permitted host/toolchain/build policy"
      pattern: "--row"
    - from: "package.json"
      to: "native/support-matrix.json"
      via: "engines.node equals nodePolicy.engine"
      pattern: "nodePolicy.engine"
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

The historical MVP goal-format warning remains: the Roadmap goal is outcome-shaped but not in the required user-story syntax. This gap plan does not invent or rewrite that approved goal.

<objective>
Implement the post-Wave-12 support amendment before ownership or staging migration resumes.

Purpose: Replace the one-tuple-per-architecture branches that reject the actual development machine with a support-row contract that can represent multiple environments for one native target without weakening fail-closed behavior.
Output: A row-driven support matcher, builder, loader, manifest identity, package engine, and a real Node-API 8 local tracer on macOS 26.5.1 / Node 25.2.1.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-12-SUMMARY.md
@.planning/debug/wave12-native-test-gate.md
@native/support-matrix.json
@src/filesystem/contained-fs.ts
@native/build.mjs
@package.json
</context>

<assumption_delta_decision>
  <generalized_primary_noun>supported native execution environment</generalized_primary_noun>
  <detected_transition>one hard-coded OS tuple per architecture to multiple support rows per target</detected_transition>
  <decision>promote</decision>
  <rationale>The user explicitly amended the 01-09 support contract after Wave 12. The support-matrix row is now the primary representation; builder and loader must consume it rather than add a second tuple-specific branch.</rationale>
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build and load the current Mac through a support row</name>
  <precondition>The execution host reports macOS 26.5.1 build 25F80, arm64, local APFS, Node 25.2.1, and the active developer directory is /Library/Developer/CommandLineTools.</precondition>
  <reversibility rating="costly">The support-row and manifest identities are consumed by later packaging/evidence plans, so undo requires a coordinated matrix, loader, builder, and evidence migration.</reversibility>
  <read_first>native/support-matrix.json; src/filesystem/contained-fs.ts; native/build.mjs; tests/unit/contained-fs.test.ts; tests/integration/init-codex-tracer.test.ts; package.json; 01-CONTAINMENT-SUPPORT.md; 01-12-SUMMARY.md; wave12-native-test-gate.md.</read_first>
  <files>native/support-matrix.json, src/filesystem/support-matrix.ts, src/filesystem/contained-fs.ts, native/build.mjs, tests/unit/containment-support.test.ts</files>
  <behavior>
    - The row `ENV-MA25` exactly represents macOS 26.5.1 / 25F80, arm64, local APFS, Node 25.2.1.
    - Its usable build provenance is Command Line Tools at `/Library/Developer/CommandLineTools`, clang++ at `/Library/Developer/CommandLineTools/usr/bin/clang++`, Apple clang 17.0.0 `clang-1700.6.4.2`, macOS SDK 26.2 build 25C58, with no full-Xcode claim.
    - A release provider compiled with official checksum-verified Node 20.19.0 headers and `NAPI_VERSION=8` loads on the live Node 25.2.1 runtime, whose observed Node-API version is 10.
    - A packed initializer happy path reaches native containment and creates only the approved minimal artifacts and selected adapter.
  </behavior>
  <action>Start with failing support-policy and local-tracer assertions. Upgrade `native/support-matrix.json` to a versioned schema that gives every row an immutable `id`, native `target`, exact runtime observation fields, and structured build policy. Add `ENV-MA25` with the exact values in the behavior block and `runnerKind: local`; retain all eight existing rows unchanged in obligation and identity. Create `src/filesystem/support-matrix.ts` as the sole parser/matcher. Change `contained-fs.ts` to observe OS/CPU/filesystem/libc/Node read-only, resolve exactly one row, then select a manifest entry by `supportRowId` plus target; two rows may share `darwin-arm64`, so target alone is no longer unique. Change `native/build.mjs` to require `--row`, validate the live host and that row's build policy, and write the provider under a row-qualified in-package path. For `ENV-MA25`, use `xcode-select`, `xcrun`, clang++, and SDK queries; do not call `xcodebuild` or represent Command Line Tools as full Xcode. Keep the approved Node 20.19.0 header SHA-256 and `NAPI_VERSION=8`; local Node 25 headers do not replace the ABI build input. Emit `supportRowId`, compiler path/version, developer directory, SDK path/version/build, source hashes, build commit, and provider hash in manifest/provenance. Build a real release provider and run the existing packed initializer happy path under Node 25.2.1. No OS/Node check may be deleted or bypassed to make the row pass.</action>
  <verify>
    <automated>node native/build.mjs --variant release --row ENV-MA25 --out . --headers "$EXSPECSO_NODE_HEADERS" &amp;&amp; npm run build &amp;&amp; npm test -- --run tests/integration/init-codex-tracer.test.ts -t "creates only the canonical foundation and Codex adapter" --testTimeout 60000</automated>
  </verify>
  <acceptance_criteria>The generated manifest names `ENV-MA25`, `napiVersion: 8`, the row-qualified provider path and exact CLT/SDK provenance; the provider loads under Node 25.2.1 and the packed initializer happy path passes without any development-only override.</acceptance_criteria>
  <done>The actual development machine has a production support-row path from matrix through build, manifest, loader, and packed initialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the complete declared and undeclared tuple invariant</name>
  <read_first>native/support-matrix.json; src/filesystem/support-matrix.ts; src/filesystem/contained-fs.ts; tests/unit/containment-support.test.ts; package.json; approved Node policy in 01-CONTAINMENT-SUPPORT.md.</read_first>
  <files>native/support-matrix.json, src/filesystem/support-matrix.ts, src/filesystem/contained-fs.ts, tests/unit/containment-support.test.ts, package.json</files>
  <behavior>
    - The named Node lanes are exactly `20.19.0`, `20.19.5`, `20.20.2`, `22.13.0`, `22.23.2`, `24.0.0`, `24.20.0`, `25.2.1`, `26.0.0`, and `26.8.1`.
    - `engines.node` is `^20.19.0 || ^22.13.0 || ^24.0.0 || 25.2.1 || ^26.0.0`; the exact Node 25 exception does not admit other odd or future majors.
    - Every declared row fixture and every named Node lane satisfies the support policy when all observations match.
    - Missing, duplicate, malformed, or mismatched row/manifest observations and Node 25 versions other than 25.2.1 are rejected before native open, lease acquisition, staging, or canonical writes.
  </behavior>
  <action>Represent Node support in matrix data with `engine`, structured even-major ranges, `exactVersions: ["25.2.1"]`, the ten `testedVersions`, and `napi: 8`; make `package.json` carry the exact same engine string. Implement the pure `isSupportedNodeVersion` and `resolveSupportRow` contracts without a list of platform-row conditionals in the loader. Add table-driven fixtures for every declared row and named Node lane, then mutate one field at a time: row ID, target, OS version/build, CPU, libc, filesystem, Node version, manifest `supportRowId`, provider path, N-API version and duplicate row. Assert a unique matching declaration accepts and every undeclared/ambiguous tuple rejects before a native provider is required. Include explicit rejections for below-floor Node, 21, 23, 25.2.0, 25.2.2, 27, and a future major. Preserve binary length/hash, package version/build commit, realpath, release/test variant, local-filesystem and Node-API checks. Do not introduce a semver dependency or a fallback that converts parse/observation failure into support.</action>
  <verify>
    <automated>npm test -- --run tests/unit/containment-support.test.ts tests/unit/contained-fs.test.ts</automated>
  </verify>
  <acceptance_criteria>The matrix has at least nine unique rows and ten unique named Node lanes; all declared fixtures pass, all undeclared/ambiguous fixtures fail closed, `package.json#engines.node` equals `nodePolicy.engine`, and no hard-coded `process.arch === ... ? [osVersion, osBuild]` support table remains in `contained-fs.ts` or `build.mjs`.</acceptance_criteria>
  <done>Support is data-driven, exact, and fail-closed while retaining every pre-existing row and lane plus the new local tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Live host observations and package manifests cross into build/load authority before the native root capability opens.

## STRIDE Threat Register — ASVS Level 1; block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-01 | Spoofing | runtime support-row selection | high | mitigate | Exact read-only environment observation, unique row resolution, row-bound manifest identity, and rejection before native open |
| T-19-02 | Tampering | provider manifest/path | high | mitigate | Row-qualified in-package paths, package/build/length/hash checks, and realpath containment |
| T-19-03 | Elevation of privilege | undeclared Node/OS tuple | high | mitigate | Matrix-derived engine/Node matcher and table-driven undeclared-tuple rejection before mutation |
| T-19-04 | Information disclosure | build provenance | low | accept | Provenance contains public tool paths/versions and hashes but no credentials or user file content |
</threat_model>

## API coverage contribution

The deterministic detector returned `detected: false`: this phase integrates no external API/SDK/service. It uses local Node/npm, filesystem primitives, and maintainer build inputs, so no `COVERAGE.md` capability matrix is created.

## Artifacts this phase produces

- `native/support-matrix.json` schema version with `rows[].id`, `rows[].target`, `rows[].runnerKind`, `rows[].buildPolicy`, `nodePolicy.engine`, `nodePolicy.ranges`, `nodePolicy.exactVersions`, `nodePolicy.testedVersions`, and `nodePolicy.napi`.
- `src/filesystem/support-matrix.ts` with `SupportMatrix`, `SupportRow`, `RuntimeObservation`, `loadSupportMatrix`, `resolveSupportRow`, and `isSupportedNodeVersion`.
- `native/build.mjs --row <support-row-id>` row-selected build contract and row-qualified manifest/provider paths.
- Manifest `supportRowId` and structured CLT/SDK provenance fields.
- `tests/unit/containment-support.test.ts` declared/undeclared tuple invariant suite.

<verification>Run the tracer command, focused support/provider tests, `npm run build`, and `git diff --check`. The generated release provider must be present for the local tracer; a mocked loader or copied old-row binary is not evidence.</verification>

<success_criteria>The exact macOS 26.5.1 / arm64 / APFS / Node 25.2.1 tuple builds and loads a Node-API 8 provider, all previous obligations remain, and support is selected only through declared rows.</success_criteria>

<output>Create `.planning/phases/01-initialize-canonical-projects/01-19-SUMMARY.md` with red/green tests, exact local build provenance, provider/manifest hashes, and the support-policy invariant results.</output>
