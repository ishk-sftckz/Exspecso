---
phase: 01-initialize-canonical-projects
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/cli/main.ts
  - src/init/run-init.ts
  - src/filesystem/git-root.ts
  - tests/helpers/git-fixture.ts
  - tests/helpers/run-cli.ts
  - tests/integration/init-codex-tracer.test.ts
  - tests/integration/init-nested.test.ts
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-05, SETUP-06, SETUP-08, ART-01, ART-02]
nyquist_compliant: true
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[EDGE-01 SETUP-01 unclassified → explicit] From a Git repository root, a packed `npx exspecso init --agent codex` invocation exits 0 and creates exactly the approved canonical foundation plus the Codex adapter."
    - "[EDGE-02 SETUP-02 unclassified → explicit] From any nested directory, init writes to the nearest containing Git root and never creates a nested `.exspecso` tree."
    - "[EDGE-08 SETUP-08 unclassified → explicit] Success output prints canonical `/exspecso-start` first and then only `$exspecso-start` for the selected Codex runtime."
    - "[EDGE-10 ART-02 unclassified → explicit] The initialized project works from repository JSON/Markdown and native adapter files without a database, cloud account, hidden canonical state, duplicate view, or export."
  artifacts:
    - path: "package.json"
      provides: "One npm package, build/test scripts, and `exspecso` bin"
      contains: "\"exspecso\""
    - path: "vitest.config.ts"
      provides: "Vitest 4 execution contract loaded by focused Wave 0 test commands"
      contains: "defineConfig"
    - path: "src/cli/main.ts"
      provides: "Executable CLI boundary"
      exports: ["main"]
    - path: "src/init/run-init.ts"
      provides: "Tracer init orchestration and real Codex artifact transaction"
      exports: ["runInit"]
    - path: "src/filesystem/git-root.ts"
      provides: "Containing Git-root resolution"
      exports: ["findGitRoot"]
    - path: "tests/integration/init-codex-tracer.test.ts"
      provides: "Packed-package end-to-end tracer proof"
    - path: "tests/helpers/git-fixture.ts"
      provides: "Reusable isolated temporary Git repository fixture with root, nested, nested-repository, and no-repository setup"
    - path: "tests/helpers/run-cli.ts"
      provides: "Reusable child-process CLI harness with cwd, stdin, environment, exit-code, stdout, and stderr control"
  key_links:
    - from: "package.json"
      to: "src/cli/main.ts"
      via: "bin entry points at compiled `dist/cli/main.js`"
      pattern: "exspecso.*dist/cli/main.js"
    - from: "src/cli/main.ts"
      to: "src/init/run-init.ts"
      via: "main delegates `init` with cwd and process I/O"
      pattern: "runInit"
    - from: "src/init/run-init.ts"
      to: ".exspecso/exspecso.config.json"
      via: "staged repository-local artifact write"
      pattern: "exspecso.config.json"
    - from: "src/init/run-init.ts"
      to: ".agents/skills/exspecso-start/SKILL.md"
      via: "selected Codex adapter generation"
      pattern: "agents/skills/exspecso-start"
  prohibitions:
    - statement: "Initialization MUST NOT introduce a database, cloud state, hidden canonical store, generated duplicate view, or export requirement as a substitute for the repository files the user inspects."
      requirement: "ART-02"
      category: "transparency"
      status: "unverified"
      flagged: true
---

## Phase Goal

**As an** Exspecso user, **I want to** initialize a project with only my selected runtime adapters and durable canonical foundations, **so that** project truth remains portable and inspectable.

<objective>
Prove the whole Phase 1 architecture through one production-quality `npx exspecso init --agent codex` path, then extend repository-root handling to nested invocation and missing-repository failure.

Purpose: Establish the walking skeleton before horizontal expansion: packaged CLI → containing Git root → selected runtime intent → canonical JSON/Markdown → native adapter → next-operation output.
Output: One buildable npm package, a real Codex initializer tracer, reusable Git/CLI test fixtures, and root/nested integration coverage.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-PATTERNS.md
@.planning/phases/01-initialize-canonical-projects/01-VALIDATION.md
@.planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md
</context>

<interfaces>
Create and preserve these initial contracts for later plans:

- `main(argv?: string[]): Promise<number>` maps expected CLI outcomes to exit codes without throwing raw errors.
- `runInit(input: { argv: string[]; cwd: string; stdin: NodeJS.ReadableStream; stdout: NodeJS.WritableStream; stderr: NodeJS.WritableStream }): Promise<number>` owns the init request boundary.
- `findGitRoot(startPath: string): Promise<string | null>` returns the containing Git root or `null`; it never runs `git init`.
- `exspecso.config.json` fields are `schemaVersion`, `project.id`, `project.title`, `mode`, `selectedAgents`, and `onboardingStatus` per D-01 through D-04.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship the end-to-end Codex initializer tracer</name>
  <precondition>`01-01-SUMMARY.md` records human approval for `vitest@4.1.11` and `@inquirer/prompts@8.6.0`.</precondition>
  <reversibility rating="costly">D-04 and the initial config shape become shared persisted contracts; changing them later touches every initialized project and downstream parser.</reversibility>
  <read_first>
    - .planning/phases/01-initialize-canonical-projects/01-01-SUMMARY.md — assert both package approvals before install
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-01 through D-04, D-07, D-08, D-11, and D-14
    - .planning/phases/01-initialize-canonical-projects/01-RESEARCH.md — package versions, architecture diagram, and Pattern 1
    - .planning/phases/01-initialize-canonical-projects/01-SKELETON.md — exact walking-skeleton capability
    - docs/research/runtime-command-naming.md — Codex destination and `$exspecso-start` invocation
  </read_first>
  <files>package.json, tsconfig.json, vitest.config.ts, src/cli/main.ts, src/init/run-init.ts, tests/integration/init-codex-tracer.test.ts</files>
  <behavior>
    - Test 1: Pack the package, invoke its `exspecso init --agent codex` bin in a temporary Git root, and observe exit 0.
    - Test 2: The run creates only `.exspecso/exspecso.config.json`, `.exspecso/constitution.md`, and `.agents/skills/exspecso-start/SKILL.md` from the Phase 1-owned families.
    - Test 3: Config fields match D-01, mode is exactly `unclassified` per D-02, the constitution contains exactly the five D-03 framework invariant subjects, and the opaque UUID is separate from the editable repository-derived title per D-04.
    - Test 4: Output begins with `/exspecso-start`, includes `$exspecso-start`, and contains no unselected runtime invocation per D-08.
  </behavior>
  <action>Start with the failing packed-package integration test, then create one ESM TypeScript npm package targeting Node `>=20` with exact researched versions `typescript@7.0.2`, `zod@4.4.3`, `vitest@4.1.11`, and `@inquirer/prompts@8.6.0`. Add an `exspecso` bin compiled by `tsc`. Create `vitest.config.ts` with Vitest's `defineConfig` as the repository's explicit test execution contract, configure it to discover the focused `tests/**/*.test.ts` suites, and make the `npm test` script invoke Vitest so both automatic discovery and the explicit `--config vitest.config.ts` Wave 0 proof use the same contract. Implement strict `init` dispatch and a real non-interactive Codex path in `runInit`: validate `--agent codex`, require a containing `.git` root, precompute the three target contents, stage them on the repository filesystem, validate the staged config, promote the complete set, and print completion only after success. Generate the D-11 managed header with template version and SHA-256 original-body hash on the adapter; do not create an adapter manifest. Use `crypto.randomUUID` for D-04. Keep this code as the permanent orchestration skeleton that later modules are extracted behind; expected errors return stable nonzero results with zero writes.</action>
  <verify>
    <automated>npm test -- --run --config vitest.config.ts tests/integration/init-codex-tracer.test.ts</automated>
  </verify>
  <acceptance_criteria>A locally packed package executes the single Codex happy path end-to-end, the three exact outputs pass content assertions, and any preflight error leaves the fixture tree unchanged.</acceptance_criteria>
  <done>A real user can initialize one Git-root project for Codex and see the correct next operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Target the containing Git root from root or nested cwd</name>
  <read_first>
    - src/init/run-init.ts — tracer orchestration to preserve while extracting root resolution
    - tests/integration/init-codex-tracer.test.ts — packed-package fixture and assertions
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-14 missing-repository behavior
    - .planning/phases/01-initialize-canonical-projects/01-PATTERNS.md — repository-root containment pattern
  </read_first>
  <files>src/init/run-init.ts, src/filesystem/git-root.ts, tests/helpers/git-fixture.ts, tests/helpers/run-cli.ts, tests/integration/init-nested.test.ts</files>
  <behavior>
    - Test 1: Root cwd resolves to itself and initializes there.
    - Test 2: A deep nested cwd resolves to its nearest containing Git root and writes nothing below the nested directory.
    - Test 3: A cwd with no containing Git repository reports the searched path, suggests `git init` or moving to the intended repository, exits nonzero, and writes nothing per D-14.
    - Test 4: A nested Git repository is the authoritative nearest containing root; parent repositories are untouched.
  </behavior>
  <action>Extract `findGitRoot(startPath)` and reusable temporary Git/child-process helpers. Resolve the root exactly once before deriving canonical, staging, or adapter paths. Walk parents without crossing the filesystem root; detect a Git worktree marker whether `.git` is a directory or file, canonicalize the result, and pass it into `runInit`. On absence, emit one stable structured diagnostic containing the searched cwd and D-14 repair guidance; never initialize Git implicitly.</action>
  <verify>
    <automated>npm test -- --run --config vitest.config.ts tests/integration/init-codex-tracer.test.ts tests/integration/init-nested.test.ts</automated>
  </verify>
  <acceptance_criteria>Root, deep nested, nested-repository, and no-repository cases produce the exact target/zero-write behavior, and all commands finish in under 60 seconds.</acceptance_criteria>
  <done>SETUP-01 and SETUP-02 work through the packaged CLI with D-14 fail-before-write semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User cwd/argv → CLI | Untrusted path and repeated CLI inputs determine repository and selected adapter. |
| CLI plan → repository filesystem | Planned canonical and adapter writes cross into durable user-controlled files. |
| npm dependency graph → package build | Approved third-party code executes during build/test. |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | Git-root and output paths | high | mitigate | Resolve the containing root once, canonicalize every target, reject any target outside it, and prove nested/no-repository behavior before writes. |
| T-02-02 | Tampering | Existing adapter path | high | mitigate | The tracer writes only an absent Codex target; D-11 header proves ownership and later rerun handling never overwrites an unowned file. |
| T-02-03 | Denial of service | Staging/promotion interruption | high | mitigate | Precompute and validate the complete mutation set, stage on the same repository filesystem, and expose the transaction seam to Plan 06 fault injection. |
| T-02-04 | Information disclosure | CLI diagnostics | low | accept | Diagnostics expose only repository-relative paths and expected/actual contract values; no secrets exist in this phase. |
| T-02-SC | Tampering | npm installs | high | mitigate | Task precondition requires the two blocking package approvals from Plan 01; TypeScript and Zod already have OK audit verdicts. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Symbol | `main` | Async CLI entry returning a process exit code |
| Symbol | `runInit` | Init request boundary accepting argv, cwd, and I/O streams |
| Symbol | `findGitRoot` | Read-only nearest containing Git-root resolver |
| CLI command | `exspecso init` | Packaged npm bin route |
| CLI flag | repeatable `--agent <runtime>` | Tracer accepts `codex`; Plan 03 completes the enum |
| Config field | `schemaVersion` | Initial schema version integer |
| Config field | `project.id` | Opaque UUID, independent of title |
| Config field | `project.title` | Editable human-readable title |
| Config field | `mode` | Literal `unclassified` |
| Config field | `selectedAgents` | Explicit selected runtime IDs |
| Config field | `onboardingStatus` | Active onboarding state only |
| Source path | `src/cli/main.ts` | Compiled bin entry |
| Source path | `src/init/run-init.ts` | Tracer orchestration |
| Source path | `src/filesystem/git-root.ts` | Git-root resolution |
| Config path | `vitest.config.ts` | Vitest 4 execution contract explicitly loaded by focused Wave 0 commands |
| Generated path | `.exspecso/exspecso.config.json` | Canonical setup state |
| Generated path | `.exspecso/constitution.md` | Five framework invariants |
| Generated path | `.agents/skills/exspecso-start/SKILL.md` | Codex-native selected adapter |
| Test path | `tests/integration/init-codex-tracer.test.ts` | Packed-package E2E proof |
| Test path | `tests/integration/init-nested.test.ts` | Nested/missing Git-root proof |
| Test helper path | `tests/helpers/git-fixture.ts` | Isolated temporary Git repository fixture supporting root, nested, nested-repository, and no-repository cases |
| Test helper path | `tests/helpers/run-cli.ts` | Child-process packaged-CLI harness controlling cwd, stdin, environment, exit code, stdout, and stderr |

<verification>
Run `npm test -- --run --config vitest.config.ts tests/integration/init-codex-tracer.test.ts tests/integration/init-nested.test.ts`, then `npm run build` and `npm pack --dry-run`. The explicit `--config vitest.config.ts` argument must resolve and load the checked-in Vitest execution contract before Wave 0 is complete. Confirm all EDGE-01, EDGE-02, EDGE-08, and EDGE-10 assertions are explicit test cases.
</verification>

<success_criteria>
- The first executable slice is a production-quality packaged CLI path touching every Phase 1 layer.
- Root and nested invocations target exactly one containing Git repository.
- Fresh Codex initialization creates only the two canonical files and selected native adapter.
- Missing Git roots and preflight errors exit nonzero with no writes.
</success_criteria>

<output>
Create `.planning/phases/01-initialize-canonical-projects/01-02-SUMMARY.md` when done.
</output>
