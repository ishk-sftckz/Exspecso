---
phase: 01-initialize-canonical-projects
plan: 07
type: execute
wave: 7
depends_on: [01-06]
files_modified:
  - src/artifacts/resolve.ts
  - src/artifacts/validate.ts
  - src/errors/diagnostic.ts
  - tests/unit/artifacts.test.ts
  - tests/integration/validation-errors.test.ts
autonomous: true
requirements: [ART-03, ART-06, ART-08]
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[GAP-JSON-01; CR-01; EDGE-12/21] A canonical JSON object's explicitly declared invalid id or parent survives scanning as an actionable diagnostic; runInit returns nonzero without writing canonical, adapter, lock, or staging files."
    - "[GAP-JSON-02; D-15/D-17/D-20] Invalid JSON declarations aggregate with independent Markdown, config, duplicate-ID, and unknown-parent errors without inventing aliases, selecting duplicate definitions, or coercing invalid declarations into absent fields."
    - "[GAP-JSON-03; ART-06] Valid D-20 JSON definitions still resolve deterministically, and config project.id remains an opaque UUID rather than an artifact ID."
  artifacts:
    - path: src/artifacts/resolve.ts
      provides: "A lossless scan result containing valid definitions and raw-declaration diagnostics"
      exports: [scanArtifacts, scanArtifactDefinitions, resolveArtifact]
    - path: src/artifacts/validate.ts
      provides: "Canonical diagnostic aggregation consuming the scanner result before init mutation"
      exports: [validateProject]
    - path: tests/integration/validation-errors.test.ts
      provides: "Invalid JSON id/parent regressions through actual runInit, including complete byte/tree preservation"
  key_links:
    - from: src/artifacts/resolve.ts
      to: src/artifacts/validate.ts
      via: "scanArtifacts returns diagnostics before invalid fields can disappear from the definition index"
      pattern: scanArtifacts
    - from: src/init/run-init.ts
      to: src/artifacts/validate.ts
      via: "Existing validateProject gate rejects malformed declarations before buildInitPlan or commitTransaction"
      pattern: validateProject
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

This is additive gap closure of an executed phase. Preserve the approved ROADMAP goal; the historical MVP user-story formatting issue remains recorded in `01-VERIFICATION.md` and is not silently normalized here.

<objective>
Close verifier gap 1 / CR-01 by preserving malformed JSON identity declarations through scanning and rejecting them through the existing init validation boundary, per D-15, D-16, D-17, and D-20.
Purpose: A direct canonical edit must not disappear before validation can explain it.
Output: Lossless scanner diagnostics and regression evidence in the existing artifact/validation suites. No new artifact family or initializer feature.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-04-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md
@.planning/phases/01-initialize-canonical-projects/01-GAP-COVERAGE.md
</context>

<interfaces>
Existing `ArtifactDefinition` accepts only branded valid `ArtifactId` values; do not broaden that type to conceal an invalid declaration. Add `ArtifactScanResult` with readonly `definitions` and `diagnostics`, returned by `scanArtifacts(root)`. Preserve `scanArtifactDefinitions(root)` as the definitions-only compatibility wrapper for existing callers. `Diagnostic` already has code, path, optional section, expected, actual, and hint. Preserve `validateProject(root)` and `runInit(input)` public signatures. Root-level artifact `id`/`parent` declarations and nested config `project.id` are distinct namespaces.
</interfaces>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and stable artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>D-05 through D-07 already make submitted runtime choices authoritative; this repair preserves D-04 project UUIDs and D-20 artifact identity rather than changing either model.</rationale>
</assumption_delta_decision>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Reject one invalid JSON parent through the real init boundary</name>
  <read_first>
    - src/artifacts/resolve.ts — jsonDefinition currently erases invalid parent values and invalid IDs
    - src/artifacts/validate.ts — validateProject and Markdown-only validateRawArtifactIds
    - tests/integration/validation-errors.test.ts — temporary Git fixtures, byte snapshots, and runInit invocation
    - src/init/run-init.ts — existing validation-before-plan gate; preserve this caller
    - src/errors/diagnostic.ts — existing diagnostic envelope and renderer
    - .planning/phases/01-initialize-canonical-projects/01-REVIEW.md — CR-01 exact reproduction
  </read_first>
  <files>src/artifacts/resolve.ts, src/artifacts/validate.ts, tests/integration/validation-errors.test.ts</files>
  <behavior>
    - A fixture containing .exspecso/definition.json with id SPEC-001 and parent REQUIREMENT-001 produces EXSPECSO_ARTIFACT_INVALID_ID at that path and section parent, retaining the actual invalid value.
    - Calling runInit on the fixture exits nonzero, prints expected/actual/repair details, prints no completion guidance, and changes neither file bytes nor the complete directory/symlink inventory outside .git.
    - The regression fails on the current implementation before correction; an unrelated valid JSON definition remains resolvable after correction.
  </behavior>
  <action>First extend the existing integration suite with the exact CR-01 fixture and capture real stdout/stderr; strengthen its snapshot helper to include directories and symlinks so a leftover empty lock or staging tree cannot escape the no-write assertion. Run the named regression and record its failing result. Add scanArtifacts and ArtifactScanResult in resolve.ts. Parse each JSON object as unknown and inspect its own top-level id and parent fields before constructing branded definitions: a present invalid declaration emits the existing EXSPECSO_ARTIFACT_INVALID_ID envelope with the raw value and exact field location instead of being erased. Keep valid definitions separate from diagnostics. Make validateProject aggregate canonical scanner diagnostics before relationship validation; preserve unrelated parsing behavior and the definitions-only compatibility wrapper. The existing runInit validation call must now block the reproduction without application-level repair, per D-15/D-16/D-20. Do not alter the approved ID vocabulary or treat config project.id as a top-level artifact declaration.</action>
  <verify><automated>npm test -- --run tests/integration/validation-errors.test.ts -t "invalid JSON parent"</automated></verify>
  <acceptance_criteria>The named regression is observed red before the fix and green afterward; it proves the raw invalid parent reaches CLI diagnostics and the complete fixture tree remains identical.</acceptance_criteria>
  <done>The user can identify and repair the previously invisible JSON parent error before init writes anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover invalid declaration shapes and aggregate independent errors</name>
  <read_first>
    - src/artifacts/resolve.ts — scanArtifacts result and existing JSON/frontmatter/headings behavior
    - src/artifacts/validate.ts — aggregate diagnostic ownership and duplicate/parent validation
    - src/errors/diagnostic.ts — stable codes and renderer
    - tests/unit/artifacts.test.ts — exact D-20 families, duplicate resolution, and rename/order fixtures
    - tests/integration/validation-errors.test.ts — tracer snapshot and captured output
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-15 through D-17 and D-20
  </read_first>
  <files>src/artifacts/resolve.ts, src/artifacts/validate.ts, src/errors/diagnostic.ts, tests/unit/artifacts.test.ts, tests/integration/validation-errors.test.ts</files>
  <behavior>
    - Invalid id and parent strings, empty strings, null, numbers, booleans, arrays, and objects are reported as present invalid fields; an absent optional parent is not an error.
    - Both invalid fields in one document yield both independently detectable errors. Other valid definitions, Markdown duplicate IDs, valid-but-unknown parents, and config syntax/schema errors remain visible in the same aggregate result.
    - A JSON object without artifact declarations, including the valid project configuration with nested project.id UUID, does not receive a false artifact-ID error.
    - Valid JSON IDs/parents retain resolution; duplicate valid IDs still select no target per D-17. No invalid alias becomes a compatibility identity.
  </behavior>
  <action>Extend the existing suites with a table of declared field shapes and a mixed-error fixture before changing additional production behavior. Use own-property presence checks, not truthiness, and render raw invalid JSON values deterministically. Reuse the scanner diagnostic path for explicit frontmatter identity declarations so those values cannot be silently dropped either; keep ordinary prose headings outside the declared-ID vocabulary from becoming false positives. Preserve Markdown section boundaries, valid-definition ordering, all duplicate locations, and the sole ROADMAP path. Ensure each independently detectable error appears once rather than being emitted by both scanner and the old raw-validation pass. Catch malformed canonical JSON with a stable artifact parse diagnostic if the config-specific parser does not own that file; retain the existing config diagnostic codes. Keep operational staging records and non-canonical project files from being mistaken for canonical identity declarations. All invalid-field variants must reach validateProject and runInit with nonzero, byte/tree-preserving outcomes per D-15/D-16. Run the existing artifact and init suites to preserve ART-06 and D-20 behavior.</action>
  <verify><automated>npm test -- --run tests/unit/artifacts.test.ts tests/integration/validation-errors.test.ts tests/integration/init-codex-tracer.test.ts</automated></verify>
  <acceptance_criteria>The shape matrix, mixed-error aggregation, no-false-positive config case, valid resolution, and duplicate ambiguity tests pass. Existing exact-ID, lazy-artifact, rename/order, and packed-init checks remain green.</acceptance_criteria>
  <done>Invalid JSON declarations are consistently actionable rather than absent, and independent canonical errors remain aggregated without mutation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User JSON/Markdown → definition index | Untrusted declarations must retain invalid values as diagnostics. |
| Validator → init mutation | Any canonical error blocks staging and writes. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | jsonDefinition / scanArtifacts | high | mitigate | Preserve present invalid id/parent fields as raw-value diagnostics before constructing branded definitions; regression through runInit. |
| T-07-02 | Spoofing | valid-definition index | high | mitigate | Keep D-20 closed and D-17 duplicate resolution ambiguous; do not coerce aliases or guess parent identity. |
| T-07-03 | Denial of service | aggregate parser | medium | mitigate | Catch syntax failures, aggregate once in deterministic order, and reuse the existing bounded per-file scanner rather than aborting at the first error. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| New type / symbol in existing file | ArtifactScanResult / scanArtifacts | Readonly valid definitions plus actionable raw-declaration diagnostics |
| Existing symbols preserved | scanArtifactDefinitions, resolveArtifact, validateProject, runInit | Existing external signatures remain usable |
| Diagnostic | EXSPECSO_ARTIFACT_INVALID_ID | Existing code gains exact JSON field locations and raw actual values |
| Conditional new diagnostic | EXSPECSO_ARTIFACT_PARSE | Canonical non-config JSON syntax error; config retains its existing code |
| Existing test files extended | tests/unit/artifacts.test.ts; tests/integration/validation-errors.test.ts | No new fixture framework, artifact family, CLI flag, or dependency |

<verification>
Run the focused task commands, npm run build, and npm test -- --run. Preserve the three recorded verifier failures until independent re-verification; this plan's implementation only addresses gap 1. See 01-GAP-COVERAGE.md for unchanged source/edge/prohibition accounting.
</verification>

<success_criteria>
CR-01 is reproduced before correction; invalid canonical JSON id/parent declarations generate complete diagnostics and block actual init mutation; valid artifact identity and all prior tests remain intact. No new dependency, canonical field, or runtime identity is introduced.
</success_criteria>

<output>Create .planning/phases/01-initialize-canonical-projects/01-07-SUMMARY.md with red/green evidence, exact tests, and remaining phase obligations.</output>
