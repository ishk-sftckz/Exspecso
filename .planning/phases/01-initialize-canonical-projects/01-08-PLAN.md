---
phase: 01-initialize-canonical-projects
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - src/filesystem/ownership.ts
  - src/filesystem/transaction.ts
  - src/filesystem/recovery.ts
  - src/init/run-init.ts
  - tests/integration/transaction-recovery.test.ts
  - tests/integration/minimal-artifacts.test.ts
autonomous: true
requirements: [SETUP-06, ART-01, ART-07, ART-08, ART-09]
gap_closure: true
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[GAP-LOCK-01; CR-02; EDGE-06/09/20] Recovery and writers acquire one atomic ownership protocol; recovery invoked during a live writer returns busy without deleting its journal, staged bytes, or ownership record."
    - "[GAP-LOCK-02; D-18] A writer starting after an idle observation but before recovery acquisition cannot be mistaken for an interrupted transaction; it completes after the losing recovery invocation returns busy."
    - "[GAP-LOCK-03; EDGE-17/20] Competing stale-owner reclaimers cannot release or remove a subsequently published live owner; only a successfully acquired lease authorizes restoration, cleanup, or promotion."
    - "[GAP-LOCK-04; EDGE-18/22] An empty direct mutation plan remains a no-op without operational files; successful/recovered init leaves the same minimal canonical tree and no Roadmap or duplicate state."
  artifacts:
    - path: src/filesystem/ownership.ts
      provides: "Atomic publication, observation, token-specific release, and conservative stale-owner acquisition"
      exports: [acquireInitOwnership, releaseInitOwnership, inspectInitOwnership, InitOwnership]
    - path: src/filesystem/recovery.ts
      provides: "Recovery requires a held lease and exposes busy without touching live transaction evidence"
      exports: [recoverInterruptedTransaction, RecoveryResult]
    - path: tests/integration/transaction-recovery.test.ts
      provides: "Deterministic writer/recovery and stale-reclaimer interleavings"
  key_links:
    - from: src/init/run-init.ts
      to: src/filesystem/ownership.ts
      via: "One acquired lease spans recovery, current-state validation, planning, and commit"
      pattern: acquireInitOwnership
    - from: src/filesystem/recovery.ts
      to: src/filesystem/ownership.ts
      via: "Direct recovery acquires the same lease; nested recovery reuses the caller's proven lease"
      pattern: InitOwnership
    - from: src/filesystem/transaction.ts
      to: src/filesystem/ownership.ts
      via: "Standalone writers and supplied-lease writers use the identical ownership protocol"
      pattern: InitOwnership
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

The historical MVP framing warning remains unchanged. This repair is serial after 01-07 and before 01-09; it does not claim to close the separate filesystem namespace-redirection gap.

<objective>
Close verifier gap 2 / CR-02 by making one ownership acquisition, rather than a prior busy observation, authorize recovery and writer mutations per D-12, D-16, D-18, and D-19.
Purpose: A concurrent init must never destroy a live writer's staged transaction.
Output: A shared lease implementation, guarded orchestration, deterministic race tests, and preserved conservative recovery behavior.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-06-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-07-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md
@.planning/phases/01-initialize-canonical-projects/01-GAP-COVERAGE.md
</context>

<interfaces>
Add a branded, root-bound InitOwnership lease with an unpredictable token, liveness metadata, and explicit acquired/released state. acquireInitOwnership returns acquired, busy, or ambiguous; only acquired carries a lease. releaseInitOwnership accepts that lease and is idempotent for its own token only. Keep lockRelativePath as .exspecso/.init.lock, now an atomically published non-empty owner directory; recognize old regular-file markers conservatively rather than coercing them into absence. Add busy to RecoveryResult. commitTransaction and recoverInterruptedTransaction accept an internal optional already-held ownership argument; otherwise they acquire and release the same protocol themselves. A supplied lease never transfers release responsibility to a nested call. Existing public CLI flags and D-01 config are unchanged.
</interfaces>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and canonical artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>The lock is temporary operational ownership, not a new project identity or adapter-state source; D-05 through D-07 and D-20 remain unchanged.</rationale>
</assumption_delta_decision>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Serialize a real writer and recovery through one owned lease</name>
  <reversibility rating="costly">Changing the temporary lock representation requires coordinated writers/readers/recovery and conservative handling of older interrupted-operation evidence.</reversibility>
  <read_first>
    - src/filesystem/transaction.ts — acquireLock, releaseLock, removeRecoveredTransaction, transactionIsActive, and onReadyToPromote
    - src/filesystem/recovery.ts — unowned recovery entry and duplicated PID observation
    - src/init/run-init.ts — hasActiveTransaction check followed by unowned recovery
    - tests/integration/transaction-recovery.test.ts — deferred-promise barrier and existing writer exclusion fixture
    - src/filesystem/ownership.ts — create this module; no existing implementation
    - .planning/phases/01-initialize-canonical-projects/01-REVIEW.md — CR-02 exact interleaving
  </read_first>
  <files>src/filesystem/ownership.ts, src/filesystem/transaction.ts, src/filesystem/recovery.ts, src/init/run-init.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>
    - Hold commitTransaction at onReadyToPromote; direct recovery returns busy, all writer evidence remains byte-identical, and releasing the writer yields committed rather than ENOENT.
    - Pause runInit after an idle ownership observation and before acquisition; start and hold another writer; resuming init returns the stable busy diagnostic and does not touch the writer's stage or lock.
    - A lease is held continuously through recovery, validation, buildInitPlan, validateInitPlan, and commit; nested functions do not deadlock by reacquiring or prematurely releasing it.
  </behavior>
  <action>First reproduce both CR-02 interleavings with deferred-promise barriers and record red results; add an internal dependency-injected before-acquisition hook to runInit rather than timing sleeps or a public bypass flag. Implement the shared ownership protocol as atomic publication of a prebuilt non-empty directory: create a unique private candidate under .exspecso, write and sync its immutable owner-token record, then rename the candidate to .init.lock. The owner record filename includes its unpredictable UUID and carries version, PID, token, and root identity. Acquisition succeeds only when the rename publishes this candidate; an already non-empty live owner directory cannot be replaced. Treat all incomplete/unreadable ownership metadata as busy or ambiguous, never as permission. A reader/PID check is advisory only. Release unlinks only this lease's UUID-named record, then uses non-recursive rmdir; never unlink a shared current-lock pathname or recursively delete an owner directory. Refactor direct commitTransaction and recovery to acquire this same lease; remove lock deletion from removeRecoveredTransaction. In runInit, a read-only preliminary validation can reject an invalid no-recovery project without creating operational files per D-16, but successful preflight is not authorization: acquire once and repeat canonical validation/planning under ownership before mutation. For interrupted staging, recover under ownership before validating the restored set. Preserve busy and ambiguous diagnostics and completion-only-after-commit. Always release only the lease owned by the current outer call in finally.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts -t "ownership race"</automated></verify>
  <acceptance_criteria>Named ownership race tests observe the current bug before correction, then prove both direct recovery and runInit lose cleanly to a live writer without deleting evidence. The winner commits; the loser reports busy; no time-based race assumption is used.</acceptance_criteria>
  <done>A user can run competing initialization/recovery invocations without one tearing down the other's active transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make stale-owner acquisition and cleanup safe under competing recovery</name>
  <read_first>
    - src/filesystem/ownership.ts — owner publication and token-specific release from Task 1
    - src/filesystem/transaction.ts — journal cleanup and existing no-op behavior
    - src/filesystem/recovery.ts — evidence validation and prior-set restoration
    - tests/integration/transaction-recovery.test.ts — existing fault matrix and new barriers
    - tests/integration/minimal-artifacts.test.ts — exact persistent-tree assertions
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-18/D-19 ambiguity and interruption boundary
  </read_first>
  <files>src/filesystem/ownership.ts, src/filesystem/transaction.ts, src/filesystem/recovery.ts, tests/integration/transaction-recovery.test.ts, tests/integration/minimal-artifacts.test.ts</files>
  <behavior>
    - Two reclaimers observe the same dead owner; after the first removes it and publishes a new live token, the second cannot remove the new token or its directory and returns busy.
    - Process death before/after ownership publication or journal creation either permits clearly identified safe acquisition/recovery or returns ambiguous while preserving evidence; a bare PID timeout never authorizes deletion.
    - PID reuse/EPERM are conservative busy, unreadable or unexpected entries are ambiguous, and version-1 regular-file lock markers are never blindly unlinked.
    - Existing three-mode promotion fault matrix still restores the prior valid set; empty commit remains a true no-op; successful/recovered paths leave no owner candidate, lock, staging, or deeper canonical artifact.
  </behavior>
  <action>Extend the barrier-controlled suite before implementing stale handling. A stale owner is reclaimable only when its complete versioned record is the sole recognized entry and the OS reports that PID absent; PID reuse or permission ambiguity remains busy. Reclamation removes only the exact observed UUID filename, then rmdir on the empty directory, then attempts its own prepared-directory publication. This is not compare-then-unlink of a shared lock file: another publication has a different child name and a non-empty directory, so an old reclaimer cannot delete it. If a token changes, a listed child is unknown, or the platform does not preserve this non-empty-directory replacement behavior, fail closed rather than continue. A legacy regular-file marker cannot safely participate in this protocol; preserve it and any journal, report a concrete quiescence/manual-inspection recovery hint, and never automatically unlink it from a stale PID observation. Version-1 journals without such ambiguous legacy ownership remain readable. Clean only candidates belonging to the held lease or complete, positively identified dead candidates after ownership is acquired; interrupted partial candidates stay preserved and diagnostic rather than guessed away. Extend the existing kill harness to await actual child exit and cover ownership publication boundaries with explicit synchronization, in addition to the retained D-19 promotion matrix. Re-run JSON invalid-input tests to ensure ordinary invalid edits still leave no operational debris and minimal-tree tests to preserve D-01/D-03, SETUP-06, and ART-09.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts tests/integration/minimal-artifacts.test.ts tests/integration/validation-errors.test.ts</automated></verify>
  <acceptance_criteria>Competing stale reclaimers and delayed release cannot delete a newer live owner. Every supported interrupted state recovers or reports explicit ambiguity with evidence preserved. All existing per-promotion D-19 tests and minimal/no-op tests still pass; no unsupported platform or power-loss claim is added.</acceptance_criteria>
  <done>Recovery owns the state it may restore or remove, and ambiguous or legacy ownership cannot be converted silently into write permission.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Competing process → transaction ownership | Observing a PID or journal does not authorize destructive recovery. |
| Operational metadata → stale reclamation | UUID, schema, root, PID, and directory entries are untrusted until checked. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | recovery / writer handoff | high | mitigate | Both acquire the same atomically published non-empty owner directory and retain the lease across the entire mutating pipeline. |
| T-08-02 | Denial of service | stale reclaim / delayed release | high | mitigate | Remove only the observed UUID child and use non-recursive rmdir; test a new owner appearing between stale observation and removal. |
| T-08-03 | Spoofing | malformed or legacy owner | high | mitigate | Unknown entries, unreadable metadata, or unsafe legacy ownership yield busy/ambiguous with evidence retained; no bare PID-based shared unlink. |
| T-08-04 | Tampering | path namespace underneath ownership | high | transfer | 01-09 must bind ownership, staging, cleanup, and promotion mutations to validated directory handles before phase acceptance; this plan does not claim CR-03 closed. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| New file | src/filesystem/ownership.ts | Shared lease acquisition/inspection/release and conservative reclaim |
| New type / symbols | InitOwnership, acquireInitOwnership, inspectInitOwnership, releaseInitOwnership | Token-bound temporary authority; no canonical identity change |
| Existing result expanded | RecoveryResult | Adds busy; none/recovered/ambiguous retain meanings |
| Existing functions refactored | commitTransaction, recoverInterruptedTransaction, runInit, hasActiveTransaction, transactionIsActive | One lease protocol; advisory observation never authorizes mutation |
| Temporary representation | .exspecso/.init.lock/ and uniquely named candidates | Versioned non-empty owner directories, removed only by proven ownership; no persistent new state |
| Test seams | Before-acquisition/publication/reclaim barriers | Internal callbacks only, no public selection or safety-bypass flag |

<verification>
Run npm run build and npm test -- --run. Inspect named interleavings and the actual child-exit assertions, not merely source text. Preserve the 01-VERIFICATION.md failure record for independent re-verification. CR-03 remains blocking until 01-09 is implemented and verified; real-TTY UAT, prohibition acknowledgement, and $gsd-secure-phase 1 remain outstanding.
</verification>

<success_criteria>
The direct live-writer reproduction and the exact idle-observation race both pass meaningful deterministic regressions. New ownership cannot be removed by stale reclaimers or delayed release. D-18 ambiguity, D-19 scope, no-op behavior, JSON rejection, and the minimal canonical tree remain intact.
</success_criteria>

<output>Create .planning/phases/01-initialize-canonical-projects/01-08-SUMMARY.md with ownership-state cases, red/green race evidence, and unresolved CR-03/manual/security obligations.</output>
