---
phase: 01-initialize-canonical-projects
plan: 13
type: execute
wave: 13
depends_on: [01-12]
files_modified: [native/contained-fs.cc, src/filesystem/contained-fs.ts, src/filesystem/ownership.ts, src/init/run-init.ts, src/filesystem/transaction.ts, tests/integration/transaction-recovery.test.ts]
autonomous: true
requirements: [SETUP-06, SETUP-07, ART-01, ART-02, ART-07, ART-08, ART-09]
gap_closure: true
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Writer/recovery share the existing UUID ownership lease, with publication, inspection, stale reclaim and release bound to original directory objects."
    - "A stale reclaimer cannot remove a newly published live owner's different token; partial, legacy and ambiguous evidence remains preserved."
    - "Staging and backup creation never follows substituted operational paths and every created byte has identified transaction ownership."
  artifacts:
    - {path: src/filesystem/ownership.ts, provides: "Capability-owned UUID lease"}
    - {path: src/filesystem/transaction.ts, provides: "Bound staging, backups and staged reads"}
    - {path: tests/integration/transaction-recovery.test.ts, provides: "Retained ownership races plus operational-path races"}
  key_links:
    - {from: src/init/run-init.ts, to: src/filesystem/ownership.ts, via: "Same held root and lease through recovery/write", pattern: acquireInitOwnership}
    - {from: src/filesystem/transaction.ts, to: src/filesystem/contained-fs.ts, via: "Bound stage/backup creation", pattern: ContainedFilesystem}
---
<objective>
Extend the proven containment slice to lock and staging operations. Preserve D-01, D-10 through D-12, D-16, D-18 and D-19; minimal canonical artifacts and exact IDs remain unchanged. Historical MVP goal framing remains unchanged.
Purpose: Fixing final promotion alone leaves operational files exposed.
Output: Bound ownership and transaction staging using the existing policy.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-initialize-canonical-projects/01-08-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-12-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Acquire and release UUID ownership through bound directories</name>
  <read_first>src/filesystem/ownership.ts complete ownership protocol; src/init/run-init.ts lease flow; native/contained-fs.cc directory publication; existing ownership race tests; 01-08-SUMMARY.md.</read_first>
  <files>native/contained-fs.cc, src/filesystem/contained-fs.ts, src/filesystem/ownership.ts, src/init/run-init.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>OW-01 through OW-04 preserve the live writer, token-specific release/reclaim, dead-candidate handling and actual killed-owner recovery while swapping .exspecso, lock and candidate ancestors.</behavior>
  <action>First extend the existing ownership barriers for operational namespace substitution. Carry the caller's root capability into InitOwnership and retain handles for the published lock/candidate directories. Implement provider publication with atomic no-replacement semantics appropriate to each OS and test contention rather than assuming POSIX errno equivalence on Windows. Read owner records and unlink only the known UUID entry relative to the bound directory; remove empty directories without recursive deletion. Preserve sole-complete-dead-record rules, liveness ambiguity, legacy regular-file preservation, and caller-versus-owner release behavior. Recheck observable identity/token changes and fail closed; do not claim atomic conditional unlink against arbitrary same-authority entry replacement. Root/descendant relocation follows only the approved 01-09 object-authority contract. Never reconstruct an absolute path from a handle and use it for mutation.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts -t "ownership"</automated></verify>
  <acceptance_criteria>The original 01-08 assertions retain their strength on all OSes, and new bound-path tests keep external sentinels and newer live tokens unchanged.</acceptance_criteria>
  <done>Concurrent initialization ownership cannot be redirected by substituted operational paths.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Stage and validate transaction bytes through the held namespace</name>
  <read_first>src/filesystem/transaction.ts writeSynced/readOptional/stageFile/backupFile; contained-fs.ts write/create/read methods; transaction-recovery.test.ts process-fault harness.</read_first>
  <files>native/contained-fs.cc, src/filesystem/contained-fs.ts, src/filesystem/transaction.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>Stage/temp/backup ancestors swapped before creation or read cannot receive external data; exclusive creation collisions stop; short writes and sync errors retain identifiable evidence.</behavior>
  <action>Replace stage directory creation, temporary writes, backup reads/writes and staged validation with bound operations, walking one validated component at a time. Use exclusive creation for private names, write loops that handle short writes, file sync, and exact byte/hash validation through opened handles. Reject unsupported file kinds and cross-device boundaries according to the approved filesystem policy. Reuse caller ownership without reacquisition. Enumerate transaction-created entries so interrupted partial staging is recognizable without deleting unknown content. A partial unproven stage must remain ambiguous per D-18, not be swept. Keep journal meaning in TypeScript. Extend TX-01/02 and RACE-03 tests; add actual operation IDs to the test-only barrier catalogue. Release builds expose no barrier or output-path override.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts -t "staging|backup|short write"</automated></verify>
  <acceptance_criteria>Operational creation/read paths have no pathname fallback, external inventory stays unchanged, and partial evidence remains inspectable on failure.</acceptance_criteria>
  <done>Initializer output is prepared inside the held filesystem namespace without redirectable staging.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
Peer-owned names and process liveness affect ownership and staged transaction data.
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Tampering | ownership publication/reclaim | high | mitigate | Bound atomic publication, UUID-specific unlink, nonrecursive empty removal and preserved race tests |
| T-13-02 | Tampering | stage/backup namespace | high | mitigate | Exclusive single-component creates, bound read/write/hash, outside sentinels |
| T-13-03 | Denial of service | interrupted ownership | medium | mitigate | Preserve unknown/partial evidence; safe dead candidate rules and explicit diagnostic |
</threat_model>
## Artifacts this phase produces
Capability-bearing InitOwnership; provider atomic directory publication; bound stage/backup operations; expanded OW/TX/RACE operation IDs and tests. Existing operational JSON remains readable; no adapter manifest or canonical database is added.
<verification>Focused ownership/staging tests, build and full regression suite. Record actual operation IDs reached by barriers, never infer from test titles.</verification>
<success_criteria>Both ownership and staging use the bound namespace while all previous lease and ambiguity guarantees remain enforced.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-13-SUMMARY.md with ownership and staging red/green evidence.</output>
