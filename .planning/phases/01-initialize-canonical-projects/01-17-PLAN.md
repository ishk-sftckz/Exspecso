---
phase: 01-initialize-canonical-projects
plan: 17
type: execute
wave: 18
depends_on: [01-16]
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - src/filesystem/contained-fs.ts
  - src/init/run-init.ts
  - .github/workflows/containment.yml
  - .github/workflows/containment-posix-tracer.yml
  - .github/workflows/containment-windows-parity.yml
  - tests/unit/root-scoped-fs.test.ts
  - tests/integration/init-typescript-tracer.test.ts
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-06, SETUP-07, ART-01, ART-02, ART-07, ART-08, ART-09]
gap_closure: true
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[D-21] The initializer runs as a pure TypeScript/Node package with no native provider, support-row lookup, compiler/header prerequisite, prebuilt selection, or native lifecycle hook."
    - "[D-21; ART-07] Repository-root scoping, component validation, stable symlink rejection, expected-preimage checks, journaled atomic promotion, process-interruption recovery, and conservative ambiguity handling remain executable and tested."
    - "[D-21] Security reporting names Claude/Codex/OpenCode host permissions and sandboxing as the OS boundary and does not claim kernel-level, race-proof, hostile same-user, or universal-filesystem containment."
    - "[D-21] Plans 01-19 and 01-20 plus their summaries remain immutable historical evidence; revised 01-17 exclusively owns the forward cutover and must prove the TypeScript path independently."
    - "Deferred native source, tests, scripts, workflow bodies, and evidence remain in the repository as non-shipped, non-invoked historical material; all three retained containment workflows have no executable event, and physical deletion is not Phase 1 work."
  artifacts:
    - path: src/filesystem/contained-fs.ts
      provides: "Pure Node implementation of the existing repository filesystem capability contract, with no generated manifest or provider dependency"
      exports: [BoundReader, DirectoryCapability, FileCapability, RootCapability, openContainedFilesystem]
    - path: tests/unit/root-scoped-fs.test.ts
      provides: "Root, traversal, stable symlink, file-kind, and missing-entry behavior for the pure Node facade"
    - path: tests/integration/init-typescript-tracer.test.ts
      provides: "Fresh root/nested initializer and additive-rerun evidence through the pure TypeScript path"
    - path: package.json
      provides: "Positive npm package allow-list and Node 22/24 engine contract without native hooks"
  key_links:
    - from: src/init/run-init.ts
      to: src/filesystem/contained-fs.ts
      via: "runInit opens one canonical repository root through the existing capability API, now implemented only with Node filesystem/path primitives"
      pattern: "openContainedFilesystem"
    - from: src/filesystem/transaction.ts
      to: src/filesystem/contained-fs.ts
      via: "the unchanged transaction/recovery consumers receive Node-backed capabilities without importing native support metadata"
      pattern: "DirectoryCapability"
    - from: package.json
      to: dist/cli/main.js
      via: "the positive package allow-list includes the compiled CLI and declarations while excluding generated native and support-matrix output"
      pattern: "dist"
---

<objective>
Cut the active runtime, build, test, package, and expensive-workflow entry points over to the pure TypeScript/Node, host-sandbox contract in D-21 without deleting historical native material.

Purpose: Return Phase 1 to Exspecso's one-package architecture without weakening the canonical artifact, adapter, atomic-write, recovery, and Git-root behaviors already completed.
Output: A Node-backed implementation behind the existing filesystem capability API, fresh TypeScript-path tracer tests, a native-free npm candidate allow-list, and three non-runnable native containment workflows.

Per the D-21 supersession boundary, completed Plans 01-19 and 01-20 and their summaries remain immutable historical evidence of the earlier native approach. Revised Plan 01-17 exclusively owns the forward cutover away from their shipped native surfaces; this is not re-execution, and their completion status cannot evidence the new TypeScript path.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-06-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-08-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-16-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-19-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-20-SUMMARY.md

Historical native source, tests, fixtures, scripts, workflows, research, support matrices, debug sessions, CI logs, and evidence explain why D-21 was made. Except for removing every executable event from `.github/workflows/containment.yml`, `.github/workflows/containment-posix-tracer.yml`, and `.github/workflows/containment-windows-parity.yml`, leave them untouched: they are non-shipped, non-invoked historical/deferred material, not completion requirements. Their physical deletion is outside Phase 1.

<interfaces>
Preserve these existing public contracts while changing their filesystem implementation:

- src/init/run-init.ts: runInit(input: InitInput): Promise&lt;number&gt;
- src/init/plan.ts: buildInitPlan(input: BuildInitPlanInput): Promise&lt;InitPlan&gt;; validateInitPlan(plan, reader?): Promise&lt;readonly InitPlanProblem[]&gt;
- src/filesystem/transaction.ts: commitTransaction(plan, options?): Promise&lt;TransactionResult&gt;
- src/filesystem/recovery.ts: recoverInterruptedTransaction(rootInput, ownership?): Promise&lt;RecoveryResult&gt;
- src/filesystem/ownership.ts: inspectInitOwnership, acquireInitOwnership, releaseInitOwnership
- src/filesystem/safe-path.ts: assertSafeTarget, assertSafeTransactionPaths
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the initializer through the Node-backed filesystem capability seam</name>
  <reversibility rating="costly">D-21 removes a cross-module native runtime seam; undoing it would require a separately approved build, packaging, support, and evidence program.</reversibility>
  <read_first>src/filesystem/contained-fs.ts; src/init/run-init.ts; src/filesystem/safe-path.ts; src/filesystem/ownership.ts; src/filesystem/transaction.ts; src/filesystem/recovery.ts; tests/helpers/git-fixture.ts; tests/helpers/run-cli.ts; tests/integration/init-rerun.test.ts; tests/integration/transaction-recovery.test.ts; 01-CONTEXT.md D-14 through D-22; immutable 01-19-SUMMARY.md and 01-20-SUMMARY.md for superseded-interface provenance only.</read_first>
  <files>src/filesystem/contained-fs.ts, src/init/run-init.ts, tests/unit/root-scoped-fs.test.ts, tests/integration/init-typescript-tracer.test.ts</files>
  <behavior>
    - Tracer: from root and nested directories in a Git repository, the compiled CLI initializes the containing root with the two canonical files and only the selected Codex adapter, then an additive rerun preserves confirmed canonical content.
    - Root contract: every reader or writer input is a nonempty relative component; traversal, absolute paths, separators inside components, unsupported entry kinds, and stable symlink segments are rejected before access.
    - Validation contract: direct invalid canonical JSON yields stable aggregate diagnostics and no repository mutation through the new TypeScript tracer rather than the preserved native-era mixed suite.
    - Durability contract: expected preimages are rechecked before promotion; staged files and journal state remain repository-local; injected exceptions and killed-process recovery yield the previous or committed valid set or a preserved ambiguous stop.
    - Boundary contract: tests prove deterministic checks and process-failure behavior, not protection against an adversarial peer changing path resolution between checks.
  </behavior>
  <action>Start with failing root/nested CLI and root-scoped capability tests. Implement D-21 in `src/filesystem/contained-fs.ts` behind its existing `BoundReader`, `DirectoryCapability`, `FileCapability`, `RootCapability`, and `openContainedFilesystem` exports so the seven current production consumers need no edits. Replace provider loading, support-row observation, generated-manifest reads, and binary loading with Node `fs`/`path` operations rooted at one retained real repository path. Validate each relative component, reject stable symlinked existing segments and unsupported entry kinds before access, preserve file/directory capability close/read/write/sync/stat/list/create/replace/publish/unlink semantics, and translate Node failures into the established structured filesystem diagnostics. Keep `safe-path.ts`, ownership, transaction, recovery, resolver, validator, and planning contracts unchanged so expected-preimage revalidation, journal ownership, atomic promotion, and ambiguity-preserving recovery continue through the same interface. In the new integration tracer, cover invalid canonical JSON with aggregate stable diagnostics and byte-for-byte no-mutation assertions so D-15/D-16 remain proven after the native-era mixed validation suite becomes historical. Update `runInit` preflight diagnostics to describe repository access rather than a bundled provider. Per the D-21 supersession boundary, do not edit Plans 01-19/01-20 or their summaries and do not use their native completion as evidence; fresh tests in this task exclusively prove the forward TypeScript path. Do not introduce a replacement native addon, WebAssembly provider, daemon, database, hidden state, or runtime-specific canonical store.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test -- --run tests/unit/root-scoped-fs.test.ts tests/integration/init-typescript-tracer.test.ts tests/integration/init-rerun.test.ts tests/integration/transaction-recovery.test.ts</automated>
  </verify>
  <acceptance_criteria>The root/nested compiled-CLI tracer, stable path rejection, preimage protection, transaction ownership, interruption recovery, validation, and additive rerun checks pass through the Node-backed existing API without generated manifest, provider, compiler, or header access.</acceptance_criteria>
  <done>One real initializer path crosses CLI, Git-root resolution, validation, planning, ownership, atomic commit, completion output, and rerun through the pure TypeScript/Node filesystem implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Disconnect historical native material from active build, test, package, and workflow entry points</name>
  <read_first>package.json; package-lock.json root package entry; tsconfig.json; vitest.config.ts; tests/integration/init-typescript-tracer.test.ts; the top-level event blocks in `.github/workflows/containment.yml`, `.github/workflows/containment-posix-tracer.yml`, and `.github/workflows/containment-windows-parity.yml`; npm pack JSON output shape; historical native/test/evidence paths only to confirm they remain outside `files_modified`.</read_first>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, .github/workflows/containment.yml, .github/workflows/containment-posix-tracer.yml, .github/workflows/containment-windows-parity.yml, tests/integration/init-typescript-tracer.test.ts</files>
  <action>Complete the D-21 cutover only at active entry points. Set the package and lockfile root engine contract to `^22.13.0 || ^24.0.0`. Replace the broad `dist` publication entry with a positive compiled-JavaScript/declaration allow-list that explicitly omits `dist/native/**` and `dist/filesystem/support-matrix.*`; keep lifecycle scripts free of native builds, downloads, or preinstall hooks. Exclude `src/filesystem/support-matrix.ts` from the TypeScript build because no active runtime imports it. Configure Vitest to omit the six historical native suites (`containment-races`, `init-codex-tracer`, `validation-errors`, `contained-fs`, `containment-evidence`, and `containment-support`) while leaving all other tests active; the new Task 1 tests replace the required TypeScript-path tracer and root-scoping proof. Set the top-level event map to exactly `on: {}` in all three retained containment workflow files: `containment.yml`, `containment-posix-tracer.yml`, and `containment-windows-parity.yml`. This removes their push, pull-request, scheduled, manual-dispatch, reusable-workflow, and any other GitHub Actions entry point while preserving their job bodies as provenance. Add a focused repository assertion named `rejects native containment entry points in active workflows` to `tests/integration/init-typescript-tracer.test.ts`. It must enumerate every `.github/workflows/*.{yml,yaml}` file; require the three named historical containment workflows to exist with an exactly empty, non-runnable top-level event map; reject an empty event map on any fourth workflow so only those three files can be exempt; and inspect job-level and step-level `uses`, complete single-line or block `run` commands, and their `with`/`env` scalar inputs in every other workflow. Build the forbidden retained-entry-point set from repository contents rather than a hand-maintained filename list: every `native/` path is forbidden, and a retained script is forbidden when its relative filename or normalized content identifies a containment build, provider, evidence, matrix, preflight, assembly, or local containment gate entry point. Factor the scanner as a test-local helper and exercise it with active-workflow cases referencing `native/preflight-windows.mjs` and `scripts/run-local-containment-gate.mjs`; both must fail independently of the current repository workflow text, and the repository assertion must fail when any active workflow command or action references either forbidden class. Do not edit or delete `native/`, native scripts, helpers, fixtures, historical tests, planning evidence, debug records, Plans 01-19/01-20, or their summaries. Apart from the three event-map edits, the historical workflow bodies remain unchanged. Those files remain non-shipped and non-invoked; physical deletion is not Phase 1 work.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm test -- --run tests/integration/init-typescript-tracer.test.ts -t "rejects native containment entry points in active workflows" &amp;&amp; npm test -- --run &amp;&amp; npm pack --dry-run --json &gt; /tmp/exspecso-pack-01-17.json &amp;&amp; node -e "const x=require('/tmp/exspecso-pack-01-17.json')[0].files.map(f=&gt;f.path); const bad=x.filter(p=&gt;p.startsWith('native/')||p.startsWith('scripts/')||p.startsWith('tests/')||p.startsWith('dist/native/')||p.startsWith('dist/filesystem/support-matrix.')); if(bad.length) throw Error(bad.join(',')); if(!x.includes('dist/cli/main.js')) throw Error('compiled CLI missing')"</automated>
  </verify>
  <acceptance_criteria>The build, active test suite, lifecycle scripts, and npm candidate invoke no native provider, support row, containment evidence script, or compiler/header gate. Exactly the three named historical containment workflows are exempt from the directory-wide assertion, each only because its top-level event map is exactly `on: {}`. Every other `.github/workflows/*.yml` or `.yaml` file has its command/action references checked, and the focused repository test rejects every `native/` path plus every retained script discovered by filename or content as a containment build, provider, evidence, matrix, preflight, assembly, or local containment gate entry point.</acceptance_criteria>
  <done>The pure TypeScript package is the only shipped and invoked Phase 1 path; native artifacts remain inspectable historical material outside build, test, install, runtime, and workflow execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/input → Git-root resolver | Untrusted working directories and runtime selections enter deterministic initialization. |
| Repository entries → Node filesystem facade | User-editable names, symlinks, file kinds, and bytes are inspected within the resolved repository root. |
| Validated plan → transaction/recovery | Planned writes, preimages, journals, ownership records, and staged bytes acquire mutation authority. |
| Host sandbox/permissions → operating system | Claude, Codex, and OpenCode host controls are the OS-level boundary; Exspecso does not replace them. |

## STRIDE Threat Register — ASVS Level 1; block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-01 | Tampering | Root-scoped reads and writes | high | mitigate | Canonical Git root, validated components, stable symlink rejection, supported-kind checks, and expected-preimage revalidation before promotion. |
| T-17-02 | Tampering | Transaction ownership and recovery | high | mitigate | Token-bound ownership, journal/hash validation, repository-local staging, atomic rename, and ambiguity-preserving recovery. |
| T-17-03 | Tampering | Same-user path race between checks and operations | medium | accept | D-21 assigns hostile peer isolation to host permissions/sandbox; docs and tests explicitly avoid race-proof or kernel-containment claims. |
| T-17-04 | Repudiation | Plans 01-19/01-20 native completion evidence | medium | mitigate | Preserve their plans/summaries immutably, name 01-17 as exclusive supersession owner, and require fresh TypeScript-path test/build/package evidence. |
</threat_model>

## Historical native material retained outside the shipped path

- Native C/C++ sources, builder, support matrix, Windows preflight, package assembly, and evidence scripts remain tracked but are excluded from build/install/runtime/package entry points.
- Historical native helpers, fixtures, and race/provider/support/evidence suites remain tracked but are excluded from active tests and cannot establish revised-plan completion.
- `.github/workflows/containment.yml`, `.github/workflows/containment-posix-tracer.yml`, and `.github/workflows/containment-windows-parity.yml` remain as provenance with empty event maps; a repository-wide workflow scan prevents any active workflow from referencing or dispatching their native matrix, build, or evidence paths.
- Plans 01-19 and 01-20, their summaries, `.planning` evidence, debug sessions, and CI logs remain immutable historical records.

Physical deletion of this deferred material is not Phase 1 work.

<verification>
Run the focused TypeScript tracer first, then the active test suite, TypeScript build, npm dry-run inventory, and workflow static assertion. Verify the npm candidate has no native/provider/support-matrix/test/script payload, all three retained containment workflow files have empty event maps, and no active workflow references a native matrix, build, or evidence path. Historical repository files remain present but non-shipped and non-invoked. Fresh passing commands from this execution are the only completion evidence.
</verification>

<success_criteria>
- The CLI initializes root and nested repositories through pure TypeScript/Node.
- Selected-adapter, rerun, validation, minimal-artifact, ownership, atomic-write, and recovery behaviors remain passing.
- Native runtime/build/certification assets remain historical files but are disconnected from build, test, install, runtime, package, and workflow execution.
- D-21's security boundary is explicit and no stronger filesystem claim is implied.
- Plans 01-19/01-20 and their summaries remain immutable history; revised 01-17 alone owns and proves the forward TypeScript cutover.
</success_criteria>

<output>
Create .planning/phases/01-initialize-canonical-projects/01-17-SUMMARY.md only after both tasks pass, recording the active-entry-point cutover, fresh verification commands, preserved behaviors, D-21 limitations, the 01-19/01-20 supersession boundary, and retained non-shipped historical paths.
</output>
