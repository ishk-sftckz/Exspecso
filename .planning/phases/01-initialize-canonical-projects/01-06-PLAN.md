---
phase: 01-initialize-canonical-projects
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified:
  - src/filesystem/safe-path.ts
  - src/filesystem/transaction.ts
  - src/filesystem/recovery.ts
  - src/init/plan.ts
  - src/init/run-init.ts
  - tests/integration/transaction-recovery.test.ts
  - tests/integration/minimal-artifacts.test.ts
autonomous: true
requirements: [SETUP-06, ART-01, ART-02, ART-07, ART-08, ART-09]
nyquist_compliant: true
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[EDGE-06 SETUP-06 concurrency → explicit] Concurrent or repeated init invocations produce only the two approved canonical files plus explicitly selected adapters; one writer owns the transaction and no invocation materializes deeper artifacts."
    - "[EDGE-09 ART-01 concurrency → explicit] A concurrent inspector sees a fully valid prior or committed artifact set, or an explicit transaction-in-progress diagnostic; it never accepts a mixed generation as canonical truth."
    - "[EDGE-17 ART-07 adjacency → explicit] Injected interruption immediately before, between, and after every adjacent promotion step leaves or recovers one fully valid previous or new set, never a mixed accepted set."
    - "[EDGE-18 ART-07 empty → explicit] An empty mutation plan is a successful no-op that creates no lock, staging directory, journal, backup, or canonical change."
    - "[EDGE-19 ART-07 ordering → explicit] Journal state records deterministic promotion order and completed step before recovery decisions; recovery validates hashes rather than inferring progress from directory order."
    - "[EDGE-20 ART-07 concurrency → explicit] Concurrent writers cannot interleave: one acquires the repository-local exclusive transaction lock and every other writer returns a stable busy result with zero writes."
    - "[EDGE-22 ART-09 concurrency → explicit] No fresh, repeated, concurrent, failed, or recovered init creates `.exspecso/roadmap.md`, a numbered Roadmap folder, an active-Roadmap selector, or a separate Roadmap status file."
  artifacts:
    - path: "src/filesystem/safe-path.ts"
      provides: "Repository-root containment and symlink/traversal rejection"
      exports: ["assertSafeTarget", "assertSafeTransactionPaths"]
    - path: "src/filesystem/transaction.ts"
      provides: "Journaled staged multi-file commit and writer exclusion"
      exports: ["commitTransaction", "TransactionJournal", "TransactionResult"]
    - path: "src/filesystem/recovery.ts"
      provides: "Conservative hash-validated interrupted-transaction recovery"
      exports: ["recoverInterruptedTransaction", "RecoveryResult"]
    - path: "tests/integration/transaction-recovery.test.ts"
      provides: "Every-step fault, tampering, concurrency, and recovery proof"
    - path: "tests/integration/minimal-artifacts.test.ts"
      provides: "Minimal-tree and sole-reserved-Roadmap negative proof"
  key_links:
    - from: "src/init/plan.ts"
      to: "src/filesystem/safe-path.ts"
      via: "every target and expected preimage is contained before staging"
      pattern: "assertSafeTransactionPaths"
    - from: "src/init/run-init.ts"
      to: "src/filesystem/recovery.ts"
      via: "startup recovery finishes or fails closed before current-state validation"
      pattern: "recoverInterruptedTransaction"
    - from: "src/init/run-init.ts"
      to: "src/filesystem/transaction.ts"
      via: "only a validated conflict-free InitPlan crosses into commit"
      pattern: "commitTransaction"
    - from: "src/filesystem/recovery.ts"
      to: "src/artifacts/validate.ts"
      via: "previous or promoted canonical set must validate before debris removal"
      pattern: "validateProject"
  prohibitions:
    - statement: "Completion and documentation MUST NOT claim physical power-loss durability or universal cross-platform filesystem guarantees from process-interruption fault tests alone; only the tested failure modes may be reported as proven."
      requirement: "ART-07"
      category: "transparency"
      status: "unverified"
      flagged: true
---

## Phase Goal

**As an** Exspecso user, **I want to** initialize a project with only my selected runtime adapters and durable canonical foundations, **so that** project truth remains portable and inspectable.

<objective>
Complete Phase 1 with repository-contained staged transactions, deterministic fault injection, writer exclusion, and conservative next-invocation recovery that preserves the last valid artifact set.

Purpose: Ensure safe reruns and direct edits cannot become partial, path-escaped, symlink-redirected, or ambiguously recovered canonical state.
Output: Safe-path enforcement, a journaled transaction state machine, fail-closed recovery, concurrency control, and the full minimal-artifact/recovery integration gate.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-PATTERNS.md
@.planning/phases/01-initialize-canonical-projects/01-VALIDATION.md
@.planning/phases/01-initialize-canonical-projects/01-04-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-05-SUMMARY.md
</context>

<interfaces>
- `TransactionJournal` contains schema version, transaction ID, repository root fingerprint, allowed relative targets, preimage hashes/absence, staged hashes, backup paths/hashes, deterministic promotion order, and completed step.
- `commitTransaction(plan, { faultAt? })` returns `committed | no-op | busy | failed`; the injected fault seam is test-only and names every state transition.
- `recoverInterruptedTransaction(root)` returns `none | recovered | ambiguous`; ambiguous state is non-mutating and blocking.
- All transaction/staging/lock paths are temporary operational state, never canonical project state, and must be absent after successful commit or validated recovery.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit one repository-contained mutation transaction</name>
  <reversibility rating="costly">The journal and recovery state machine must remain readable across package upgrades while transactions are in flight; changing it requires coordinated backward-compatible recovery handling.</reversibility>
  <read_first>
    - src/init/plan.ts — immutable writes, preimages, conflicts, and selected targets
    - src/init/run-init.ts — current staging/promotion seam to replace
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-12 and D-18
    - .planning/phases/01-initialize-canonical-projects/01-RESEARCH.md — Pattern 3, Pitfall 1, and ART-07 open question
    - .planning/phases/01-initialize-canonical-projects/01-VALIDATION.md — transaction fault-injection requirements
  </read_first>
  <files>src/filesystem/safe-path.ts, src/filesystem/transaction.ts, src/init/plan.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>
    - Test 1: Traversal, absolute external target, symlinked parent/target, and changed preimage are rejected before staging with zero writes.
    - Test 2: Empty plan returns no-op without creating operational debris.
    - Test 3: One writer acquires an exclusive repository-local lock; concurrent writers return busy without interleaving.
    - Test 4: Every output is staged on the repository filesystem, flushed, hash-validated, journaled with deterministic order/completed step, then promoted.
    - Test 5: A fault injected at every transition leaves enough validated journal/backup evidence for deterministic recovery and never reports completion prematurely.
  </behavior>
  <action>Implement root-relative safe-target checks with `lstat`/`realpath`: reject traversal, external absolute paths, symlink ambiguity at any existing path segment, unsupported target types, and preimage drift. Build `commitTransaction` around exclusive lock creation, a private same-filesystem staging directory, explicit journal, staged and backup hashes, file/directory flushes where Node supports them, deterministic promotion order, and a recorded completed step. Validate staged artifacts before the first promotion. Expose named injected fault points before/after every state transition for deterministic tests. Treat an empty plan as a true no-op. Do not use unbounded recursive deletion or accept per-file success as transaction completion.</action>
  <verify>
    <automated>npm test -- --run tests/integration/transaction-recovery.test.ts</automated>
  </verify>
  <acceptance_criteria>Containment/symlink/preimage cases fail before staging, empty plan leaves no debris, concurrent writers never interleave, all outputs are validated before promotion, and each injected transition failure is recoverable from explicit journal evidence.</acceptance_criteria>
  <done>The complete init plan crosses the filesystem boundary through one contained, exclusive, hash-checked transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover only clearly identified interrupted staging</name>
  <read_first>
    - src/filesystem/transaction.ts — journal, lock, backup, and fault-point contracts from Task 1
    - src/artifacts/validate.ts — canonical set validation gate
    - src/init/run-init.ts — startup sequence and diagnostic rendering
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-18 fail-closed recovery rule
    - .planning/REQUIREMENTS.md — SETUP-06, ART-01, ART-02, ART-07, ART-08, ART-09
  </read_first>
  <files>src/filesystem/recovery.ts, src/filesystem/transaction.ts, src/init/run-init.ts, tests/integration/transaction-recovery.test.ts, tests/integration/minimal-artifacts.test.ts</files>
  <behavior>
    - Test 1: For each injected fault, next init validates journal root/paths/hashes/step, restores or completes one valid set, reports recovery, and removes only that identified staging debris per D-18.
    - Test 2: Tampered journal, unexpected path, mismatched staged/backup/canonical hash, or external canonical change returns ambiguous, leaves all evidence untouched, exits nonzero, and performs no new init writes.
    - Test 3: Readers encountering an active valid transaction report busy instead of accepting mixed generations; after recovery they validate one complete set.
    - Test 4: Successful/recovered fresh and rerun cases contain only config, constitution, selected adapters, and no persistent staging/lock/journal state.
    - Test 5: No execution path creates any Roadmap/status/selector or other deferred artifact family.
  </behavior>
  <action>Run `recoverInterruptedTransaction` before normal current-state validation. Accept cleanup or restoration only when the journal is inside the containing Git root, every allowed path is contained and non-symlinked, all preimage/staged/backup/current hashes match an allowed state, and the resulting canonical set passes `validateProject`. Report the recovered transaction ID and disposition. If any evidence is missing, tampered, ambiguous, or externally changed, preserve it, return stable diagnostics, and stop before new writes per D-18. Make readers detect active transaction state and return a typed busy diagnostic rather than treating an in-progress mixed filesystem view as canonical. Add final minimal-tree assertions across success, concurrency, failure, and recovery. Scope claims to automated process interruption; do not label physical power-loss behavior proven without the later platform evidence identified by research.</action>
  <verify>
    <automated>npm test -- --run tests/integration/transaction-recovery.test.ts tests/integration/minimal-artifacts.test.ts tests/integration/init-rerun.test.ts tests/integration/validation-errors.test.ts</automated>
  </verify>
  <acceptance_criteria>Every injected interruption recovers or retains one valid set, ambiguous/tampered evidence fails closed without cleanup, readers never accept a mixed set, operational debris is absent after success, and all prohibited artifact paths remain absent.</acceptance_criteria>
  <done>D-18 and ART-07 are proven for declared process-interruption failure points without overstating physical durability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Planned relative paths → filesystem objects | Target paths may traverse, escape, or be redirected by symlinks. |
| Staging/journal/backup bytes → recovery decision | Operational debris is untrusted until root, path, step, and hash evidence validate. |
| Concurrent CLI process → transaction lock | Multiple writers/readers may observe the repository during mutation. |

## STRIDE Threat Register (ASVS Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | root containment/symlink paths | high | mitigate | Canonicalize the Git root, validate every path segment with `lstat`/`realpath`, reject traversal/external/symlink targets before staging, and recheck before promotion. |
| T-06-02 | Tampering | journal/staged/backup data | high | mitigate | D-18 recovery requires exact transaction schema, allowed paths, preimage/staged/backup/current hashes, completed step, and valid resulting canonical set; ambiguity preserves evidence and blocks. |
| T-06-03 | Denial of service | concurrent init processes | high | mitigate | Exclusive repository-local lock permits one writer; other writers/readers receive stable busy results and cannot accept/interleave mixed state. |
| T-06-04 | Tampering | duplicate IDs after recovery | high | mitigate | Validate the entire restored/promoted canonical set with duplicate-ID rejection before cleanup or new writes. |
| T-06-05 | Repudiation | recovery result | low | accept | CLI reports transaction ID and disposition and the verified Task commit captures tests; a separate canonical recovery log would violate minimal state. |
| T-06-06 | Information disclosure | journal contents | low | accept | Journal contains repository-relative paths and hashes only, no secrets; it is deleted after validated success/recovery. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Type | `TransactionJournal` | Version, transaction/root IDs, allowed paths, hashes, order, completed step |
| Type | `TransactionResult` | `committed | no-op | busy | failed` outcome |
| Type | `RecoveryResult` | `none | recovered | ambiguous` outcome |
| Symbol | `assertSafeTarget` | Single target containment/symlink/preimage assertion |
| Symbol | `assertSafeTransactionPaths` | Complete-plan path gate |
| Symbol | `commitTransaction` | Exclusive stage/validate/journal/promote/cleanup state machine |
| Symbol | `recoverInterruptedTransaction` | Hash-validated next-invocation recovery |
| Temporary path | `.exspecso/.staging/<transaction-id>/` | Same-filesystem staged bodies/backups/journal; never canonical and removed only after validation |
| Temporary path | `.exspecso/.init.lock` | Exclusive writer marker; never canonical |
| Test path | `tests/integration/transaction-recovery.test.ts` | Fault, tamper, traversal, symlink, empty, ordering, and concurrency suite |
| Test path | `tests/integration/minimal-artifacts.test.ts` | Exact persistent tree and absent Roadmap/deeper-family assertions |

## Multi-Source Coverage Audit

| Source | ID/Item | Covered by | Status |
|---|---|---|---|
| GOAL | Initialize with only selected adapters and durable canonical foundations | 01-02 through 01-06 | COVERED |
| REQ | SETUP-01, SETUP-02 | 01-02 | COVERED |
| REQ | SETUP-03, SETUP-04 | 01-01, 01-03 | COVERED |
| REQ | SETUP-05, SETUP-08 | 01-02, 01-03 | COVERED |
| REQ | SETUP-06 | 01-02, 01-06 | COVERED |
| REQ | SETUP-07 | 01-05 | COVERED |
| REQ | ART-01, ART-02 | 01-02, 01-04, 01-06 | COVERED |
| REQ | ART-03, ART-04, ART-05, ART-06 | 01-01, 01-04 | COVERED |
| REQ | ART-07 | 01-06 | COVERED |
| REQ | ART-08 | 01-01, 01-04, 01-06 | COVERED |
| REQ | ART-09 | 01-04, 01-06 | COVERED |
| CONTEXT | D-01 through D-04 initial artifacts/identity | 01-02, 01-04 | COVERED |
| CONTEXT | D-05 through D-08 selection/completion | 01-03 | COVERED |
| CONTEXT | D-09 through D-13 adapter refresh | 01-05 | COVERED |
| CONTEXT | D-14 missing Git root | 01-02 | COVERED |
| CONTEXT | D-15 through D-17 validation/duplicates | 01-04 | COVERED |
| CONTEXT | D-18 conservative recovery | 01-06 | COVERED |
| RESEARCH | Package legitimacy audit | 01-01 gate; 01-02 precondition | COVERED |
| RESEARCH | Node/TypeScript/Zod/Vitest/Inquirer one-package stack | 01-02, 01-03, 01-04 | COVERED |
| RESEARCH | Git-root containment and plan-before-write | 01-02, 01-05, 01-06 | COVERED |
| RESEARCH | Native adapter registry and managed headers | 01-03, 01-05 | COVERED |
| RESEARCH | Schema/resolver/aggregate diagnostics | 01-04 | COVERED |
| RESEARCH | Journaled staged transaction and fault injection | 01-06 | COVERED |
| RESEARCH | No physical power-loss overclaim | 01-06 prohibition and acceptance criteria | COVERED |
| RESEARCH | No external API or ORM schema push | `COVERAGE.md` | COVERED |

## Specless Probe Accounting

| Probe candidate | Authored predicate | Location |
|---|---|---|
| SETUP-01 unclassified | EDGE-01 | 01-02 truths |
| SETUP-02 unclassified | EDGE-02 | 01-02 truths |
| SETUP-03 unclassified | EDGE-03 | 01-03 truths |
| SETUP-04 unclassified | EDGE-04 | 01-03 truths |
| SETUP-05 concurrency | EDGE-05 | 01-03 truths |
| SETUP-06 concurrency | EDGE-06 | 01-06 truths |
| SETUP-07 unclassified | EDGE-07 | 01-05 truths |
| SETUP-08 unclassified | EDGE-08 | 01-02 truths |
| ART-01 concurrency | EDGE-09 | 01-06 truths |
| ART-02 unclassified | EDGE-10 | 01-02 truths |
| ART-03 adjacency | EDGE-11 | 01-04 truths |
| ART-03 empty | EDGE-12 | 01-04 truths |
| ART-03 ordering | EDGE-13 | 01-04 truths |
| ART-04 unclassified | EDGE-14 | 01-04 truths |
| ART-05 unclassified | EDGE-15 | 01-04 truths |
| ART-06 concurrency | EDGE-16 | 01-04 truths |
| ART-07 adjacency | EDGE-17 | 01-06 truths |
| ART-07 empty | EDGE-18 | 01-06 truths |
| ART-07 ordering | EDGE-19 | 01-06 truths |
| ART-07 concurrency | EDGE-20 | 01-06 truths |
| ART-08 unclassified | EDGE-21 | 01-04 truths |
| ART-09 concurrency | EDGE-22 | 01-06 truths |

No-silent-drop equality: 22 probe candidates = 22 authored `must_haves.truths` predicates + 0 flagged edge assumptions.

Adversarial prohibition recall retained four bespoke human-control/transparency constraints, all descriptor-less and flagged-unverified: hidden duplicate state (01-02), detection-as-consent (01-03), omission-as-deletion/overwrite (01-05), and durability overclaim (01-06). Canon security candidates for path traversal, symlink redirection, duplicate-ID tampering, and package supply chain were dropped from prohibition minting and referred to the ASVS threat registers and `$gsd-secure-phase`. Precision-filtered routine correctness candidates are owned by EDGE truths and tests. No-silent-drop equality: 4 kept prohibitions = 4 `must_haves.prohibitions` entries + 0 unresolved prohibition assumptions.

<verification>
Run `npm test -- --run`, `npm run build`, and `npm pack --dry-run`. Confirm every injected transaction point, traversal/symlink/preimage mutation, concurrent writer/reader, tampered journal, minimal-tree, direct-edit, rerun, selection, root/nested, and packed tracer case is green within the 60-second validation ceiling. At end-of-phase human verification, run the real TTY selector and document the tested OS/filesystem; state only the fault modes actually proven.
</verification>

<success_criteria>
- Every Phase 1 requirement and D-01 through D-18 has automated evidence or its declared human gate.
- All 22 fallback edge candidates are explicit `must_haves.truths`; all four kept non-canon prohibitions are descriptor-less and flagged-unverified.
- Repository-root containment, symlink/path traversal, unowned overwrite, duplicate IDs, and tampered/interrupted staging are fail-closed.
- The last valid artifact set survives every declared injected process-interruption point.
- Successful init leaves only canonical files and selected native adapters, never persistent operational or deferred artifacts.
- No claim exceeds the OS/filesystem and process-interruption evidence actually run.
</success_criteria>

<output>
Create `.planning/phases/01-initialize-canonical-projects/01-06-SUMMARY.md` when done.
</output>
