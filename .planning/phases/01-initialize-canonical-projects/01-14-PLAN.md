---
phase: 01-initialize-canonical-projects
plan: 14
type: execute
wave: 16
depends_on: [01-13]
files_modified: [src/filesystem/journal.ts, src/filesystem/transaction.ts, src/filesystem/recovery.ts, src/filesystem/contained-fs.ts, tests/integration/transaction-recovery.test.ts]
autonomous: true
requirements: [SETUP-06, SETUP-07, ART-01, ART-07, ART-08, ART-09]
gap_closure: true
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every actual planned output uses a newly written sibling and bound atomic replacement; external hard-link aliases keep their old bytes."
    - "Each interruption between replacement and journal advancement is recoverable from validated prior/staged evidence or explicitly ambiguous without destructive cleanup."
    - "Recovery can itself be interrupted and restarted; it never downgrades an unsafe-path error into permission to write."
  artifacts:
    - {path: src/filesystem/journal.ts, provides: "Validated restartable operational journal transitions"}
    - {path: src/filesystem/transaction.ts, provides: "All-output bound promotion"}
    - {path: src/filesystem/recovery.ts, provides: "Bound idempotent restoration"}
  key_links:
    - {from: src/filesystem/transaction.ts, to: src/filesystem/journal.ts, via: "Write-ahead intent and observed completion", pattern: journal}
    - {from: src/filesystem/recovery.ts, to: src/filesystem/contained-fs.ts, via: "Bound evidence and temp/replace restoration", pattern: ContainedFilesystem}
---
<objective>
Complete the promotion and restartable restoration path for ART-07 and D-18/D-19. Preserve the approved stronger claim as pending if 01-09 does not accept the provider boundary. Historical MVP goal framing remains unchanged.
Purpose: The old completedStep-only journal cannot prove every replacement/journal interleaving.
Output: Restartable journal and real per-write fault coverage.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/01-initialize-canonical-projects/01-13-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Promote every output with restartable write-ahead evidence</name>
  <reversibility rating="costly">Operational journal readers and interrupted transactions must agree on versioned transitions.</reversibility>
  <read_first>transaction.ts TransactionJournal and promotion loop; recovery.ts journalShapeIsValid; 01-09 approved journal compatibility decision; transaction-recovery.test.ts existing promotion fault loops.</read_first>
  <files>src/filesystem/journal.ts, src/filesystem/transaction.ts, src/filesystem/contained-fs.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>TX-03/04 cover every fresh-init and adapter-rerun planned write, hard-linked destinations, replacement failure, and interruption before/after replacement and journal advancement.</behavior>
  <action>First enumerate the actual plan's writes in tests and inject each declared transition. Centralize strict journal parsing in journal.ts. Define versioned states prepared, promoting, restoring, and cleaning, with per-entry prior/staged hashes and an in-flight step recorded before mutation; atomically replace the journal itself through a new sibling. Record completion after replacement, while recovery accepts only explicitly modeled prior/staged hash states for the in-flight entry. Preserve legacy schema-1 recovery only when its complete evidence proves the prior state; otherwise retain it with an actionable diagnostic. New journal format is operational recovery evidence, never new canonical product truth. Recheck observed target type/identity/preimage, write a private sibling through its bound parent, sync it, then replace the target entry. Never truncate the existing inode, including a hard-linked target. Keep full staged copies until validated recovery/commit cleanup. Directory-sync capability and its errors follow the approved process-level durability contract; do not swallow an unexpected sync error or claim power-loss durability.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts -t "promotion|journal|hard link"</automated></verify>
  <acceptance_criteria>All actual writes and journal gaps are exercised; outside aliases remain unchanged. Unknown state is retained, not guessed from completedStep.</acceptance_criteria>
  <done>Every initializer output has bound replacement and sufficient process-interruption evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Restore the validated prior set through bound restartable operations</name>
  <read_first>recovery.ts validateEvidence/restorePrior; journal.ts transitions; transaction.ts removeRecoveredTransaction; transaction-recovery.test.ts actual child termination helpers.</read_first>
  <files>src/filesystem/recovery.ts, src/filesystem/journal.ts, src/filesystem/transaction.ts, tests/integration/transaction-recovery.test.ts</files>
  <behavior>RC-01 through RC-04 cover every restore entry/transition, recovery interrupted twice, changed journal/backup/canonical bytes, and containment failure; actual killed children must be observed exited before recovery.</behavior>
  <action>Read all evidence through the held capability and require exact transaction/root/entry schema and hashes. Remove the catch that replaces a failed containment check with lexical/isRegularFile checks. Record restoration intent, restore existing files via fresh sibling plus bound replacement, and remove newly created entries only under positively identified authority. Make restoration idempotent: a prior-hash entry is already restored, a staged-hash entry may be restored, any other observation is ambiguous. Record restore progress without deleting needed backups; revalidate the whole canonical set before cleanup. Exercise exceptions, simulated interruption, and actual child termination at each restore transition and a second restart. POSIX asserts observed SIGKILL; Windows uses explicit successful termination plus observed process exit and restart evidence, not a fabricated POSIX signal. Preserve the 01-08 live-owner contention oracle.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts -t "restore|recovery|killed"</automated></verify>
  <acceptance_criteria>All declared recoverable schedules restore byte-identical prior artifacts; externally changed evidence remains untouched with no completion message. Recovery never follows substituted names.</acceptance_criteria>
  <done>Users can restart interrupted initialization and interrupted recovery without unsafe restoration.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
Journal/staged evidence authorizes replacement and restoration of canonical files.
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-01 | Tampering | promotion/hard links | high | mitigate | Bound new-sibling replacement and external alias sentinel |
| T-14-02 | Tampering | recovery authority | high | mitigate | Strict journal/hash validation; no downgraded containment error |
| T-14-03 | Denial of service | mid-recovery interruption | high | mitigate | Write-ahead restore states, idempotent per-entry checks and forced process tests |
</threat_model>
## Artifacts this phase produces
journal.ts strict schema/transition helpers; transaction all-output replacement; recovery restartable restore; TX/RC/fault rows in existing recovery suite. No user artifact schema or stable-ID changes.
<verification>Run focused tests in bounded shards, build, full existing suite, and record the generated list of per-write transition cases. Every declared transition has an actual reached barrier.</verification>
<success_criteria>Promotion and restoration meet the approved object-authority and D-18/D-19 contract across every declared process boundary.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-14-SUMMARY.md with red/green transition inventory, legacy-journal disposition, and actual kill oracles.</output>
