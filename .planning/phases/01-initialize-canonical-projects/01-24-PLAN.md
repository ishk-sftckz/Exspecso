---
phase: 01-initialize-canonical-projects
plan: 24
type: execute
wave: 23
depends_on: [01-23]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
  - src/cli/main.ts
  - src/init/runtime-selection.ts
  - tests/unit/runtime-selection.test.ts
  - tests/integration/installed-cli.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-01-1]
requirements: [SETUP-03, SETUP-04, SETUP-05]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[G-01-1; SETUP-03; SETUP-04] Interactive initialization always presents Claude Code, OpenAI Codex, and OpenCode as equal unchecked choices without inspecting or labeling the installed-agent environment."
    - "[G-01-1; SETUP-05] Only the user's submitted runtime subset controls adapter creation and persisted selectedAgents; ambient agent variables have no selection or presentation role."
    - "Empty interactive submission still explains that one runtime is required, cancellation still writes nothing, and non-interactive callers still use repeatable --agent flags."
    - "The active Phase 1 intent, unit evidence, installed-package evidence, and real-TTY UAT expectation all describe the same detection-free selector."
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Detection-free Phase 1 runtime-selection success criterion"
    - path: .planning/REQUIREMENTS.md
      provides: "Revised SETUP-04 requirement preserving explicit user choice without runtime suggestions"
    - path: .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
      provides: "Superseding D-05 decision recorded from UAT gap G-01-1"
    - path: src/init/runtime-selection.ts
      provides: "Stable unchecked runtime choices with no environment-detection API or prompt metadata"
    - path: src/cli/main.ts
      provides: "Interactive selection wiring that passes no ambient agent information"
    - path: tests/unit/runtime-selection.test.ts
      provides: "Focused regression for detection-free labels, selection, empty retry, cancellation, and non-TTY flags"
    - path: tests/integration/installed-cli.test.ts
      provides: "Packed CLI evidence that ambient agent variables cannot change explicit selection outputs"
  key_links:
    - from: src/cli/main.ts
      to: src/init/runtime-selection.ts
      via: "resolveSelectedAgents receives only command arguments, TTY state, and the prompt boundary"
      pattern: "resolveSelectedAgents"
    - from: src/init/runtime-selection.ts
      to: src/init/run-init.ts
      via: "the returned submitted AgentId list remains the sole selectedAgents input"
      pattern: "selection.agents"
    - from: tests/unit/runtime-selection.test.ts
      to: .planning/phases/01-initialize-canonical-projects/01-UAT.md
      via: "automated state-machine evidence leaves only actual terminal rendering and cancellation for renewed human verification"
      pattern: "Real TTY runtime selection"
---

<objective>
Close UAT gap G-01-1 by removing installed-agent detection from runtime selection while preserving explicit user choice and selected-subset-only adapter writes.

Purpose: Align the active Phase 1 product contract with the user's later UAT decision that the initializer should simply offer all supported agents and let the user choose, without environment-derived suggestions.
Output: Revised canonical intent, a detection-free runtime-selection boundary, focused and packed-package regressions, and fresh evidence ready for real-TTY UAT.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-UAT.md
@.planning/debug/no-runtime-detection-labels.md
@.planning/phases/01-initialize-canonical-projects/01-03-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-23-SUMMARY.md
@src/cli/main.ts
@src/init/runtime-selection.ts
@src/init/run-init.ts
@tests/unit/runtime-selection.test.ts
@tests/integration/installed-cli.test.ts
@package.json

UAT gap G-01-1 is a newer explicit user decision and supersedes only the detection-label clause of Phase 1 decision D-05, SETUP-04, Roadmap success criterion 2, and their verification wording. Preserve D-05's all-supported, stable, unchecked checklist and preserve D-06 through D-08 unchanged. Historical 01-03 PLAN/SUMMARY records remain immutable evidence of what was previously approved and implemented.

Do not change the supported AgentId set or order, add defaults, preselect options, infer choices from files or environment variables, remove the at-least-one requirement, alter cancellation semantics, change repeatable --agent behavior, modify adapter paths/content, broaden replacement authority, or touch filesystem transaction behavior. This plan does not resolve the separate FIND-NNN/PAC-NNN Phase 1 concern recorded in STATE.md.
</context>

## Phase Goal

Roadmap source: Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the detection-free contract and remove prompt detection end to end</name>
  <read_first>.planning/phases/01-initialize-canonical-projects/01-UAT.md gap G-01-1; .planning/debug/no-runtime-detection-labels.md; .planning/ROADMAP.md Phase 1 success criterion 2; .planning/REQUIREMENTS.md SETUP-03 through SETUP-05; .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md D-05 through D-08; src/cli/main.ts runtime-selection import and call; src/init/runtime-selection.ts public types, choice rendering, and selection flow; tests/unit/runtime-selection.test.ts.</read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md, src/cli/main.ts, src/init/runtime-selection.ts, tests/unit/runtime-selection.test.ts</files>
  <behavior>
    - RED: the public interactive-selection input and resolver can be called without detectedAgents, and the current required detection plumbing must fail compilation or the focused assertions before production edits.
    - RED: the prompt receives exactly Claude Code, OpenAI Codex, and OpenCode in stable order, each with checked false and no environment-derived label suffix.
    - GREEN: selecting any nonempty subset returns exactly that submitted subset in submission order; no ambient environment value is read or transformed into prompt metadata.
    - GREEN: an empty interactive submission reopens with the existing explanation, cancellation returns the explicit cancelled result, and non-TTY repeatable flags retain their current diagnostics and order semantics.
  </behavior>
  <action>First make scoped canonical-intent edits: revise Phase 1 Roadmap success criterion 2 to say users choose from the three offered integrations; revise SETUP-04 so it requires equal unchecked options without installed-agent detection or suggestions; and revise D-05 to record that G-01-1 supersedes its informational-detection clause while retaining the stable all-supported checklist. Do not rewrite unrelated phase text or historical plans/summaries. Then change the focused unit contract before production code so omitting detectedAgents and expecting plain stable labels produces a genuine RED result. Remove detectAgents(), detectedAgents fields, the detected set, conditional label suffixes, and the CLI import/call; simplify choice construction around the fixed supported registry. Keep only the submitted selection flowing to runInit(), per the unchanged selectedAgents authority contract. Do not retain dormant detection helpers or compatibility overloads because they would preserve an unintended product surface.</action>
  <verify>
    <automated>npm test -- --run tests/unit/runtime-selection.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>The canonical Phase 1 intent and compiled selector agree that all three supported runtimes appear as equal unchecked options with no installed-agent inspection or label; focused tests pass for stable labels, every submitted subset, empty retry, cancellation, invalid flags, and non-TTY selection.</acceptance_criteria>
  <done>A real interactive invocation reaches a detection-free prompt and only its submitted runtime IDs can reach initialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove ambient agents cannot affect the installed package and preserve Phase 1 regressions</name>
  <read_first>tests/integration/installed-cli.test.ts package fixture, hostile-environment test, subset loop, and projectFiles assertions; tests/helpers/package-fixture.ts runInstalledCli environment behavior; src/cli/main.ts and src/init/runtime-selection.ts after Task 1; package.json scripts/files/engines; .planning/phases/01-initialize-canonical-projects/01-UAT.md renewed expected truth.</read_first>
  <files>tests/integration/installed-cli.test.ts, tests/unit/runtime-selection.test.ts</files>
  <behavior>
    - A standard packed, scripts-disabled installed CLI invoked with explicit --agent flags produces the same selectedAgents, adapter tree, and completion guidance when common agent-presence variables are populated as when they are absent.
    - Production TypeScript contains no installed-agent detection helper, detected-agent parameter, or environment-name branch used by the old label path.
    - The full active suite, TypeScript build, and npm package inventory remain green; the tarball still contains only the established pure-TypeScript/Node runtime surface.
    - Gap G-01-1 remains diagnosed until renewed real-TTY UAT confirms plain unchecked labels, empty-selection retry, cancellation with no writes, and subset-only adapter creation.
  </behavior>
  <action>Extend the existing installed-package hostile-environment or subset evidence with representative Claude Code, Codex, and OpenCode presence variables while invoking a declared explicit subset. Assert the resulting config, adapter files, and completion text remain controlled only by the flags and match the clean-environment contract. Keep real-TTY rendering as the explicit human evidence boundary; do not introduce a pseudo-terminal dependency or claim an injected prompt proves terminal rendering. Run a source-surface check for the removed detection API and environment branches, then execute the focused unit/integration tests, full active suite, build, and dry-run package inventory. Record the automated results and a human_judgment coverage item for renewed real-TTY verification in 01-24-SUMMARY.md; do not mark UAT or Phase 1 complete from automated evidence alone.</action>
  <verify>
    <automated>npm test -- --run tests/unit/runtime-selection.test.ts tests/integration/installed-cli.test.ts &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; npm pack --dry-run --json &amp;&amp; ! rg -n 'detectAgents|detectedAgents|CLAUDECODE|CLAUDE_CODE|CODEX_HOME|OPENCODE|\(detected\)' src</automated>
  </verify>
  <acceptance_criteria>The installed package remains behaviorally identical under ambient agent-presence variables, production source has no old detection path, all automated gates pass, and the execution summary explicitly routes the terminal-only presentation/cancellation check back to UAT.</acceptance_criteria>
  <done>Automated evidence proves the detection surface is absent without weakening explicit selection, package, or regression contracts; only renewed human terminal observation remains.</done>
</task>

</tasks>

## Artifacts this phase produces

- Revised active intent: `.planning/ROADMAP.md` Phase 1 success criterion 2, `.planning/REQUIREMENTS.md` SETUP-04, and `01-CONTEXT.md` D-05.
- Simplified production boundary: `src/cli/main.ts` and `src/init/runtime-selection.ts` contain no installed-agent detection or detected-label plumbing.
- Updated evidence: `tests/unit/runtime-selection.test.ts` and `tests/integration/installed-cli.test.ts` prove equal unchecked choices and ambient-environment independence.
- Execution evidence: `01-24-SUMMARY.md` carries automated coverage plus one human-judgment item for renewed real-TTY UAT.
- New runtime IDs, defaults, adapter formats, CLI flags, dependencies, schemas, services, or filesystem changes: none.

<assumption_delta_decision>
Primary noun: supported runtime selection option.
Decision: promote — UAT gap G-01-1 supersedes only the old detection-label portion of D-05/SETUP-04. The stable AgentId set, order, unchecked defaults, explicit submitted-set authority, cancellation, and repeatable non-TTY flags remain unchanged.
</assumption_delta_decision>

<api_coverage_decision>
No external API, SDK, service, endpoint, OAuth flow, webhook, or MCP surface is introduced or changed. COVERAGE.md does not apply.
</api_coverage_decision>

<schema_push_decision>
No database, ORM, migration, or persisted schema shape changes. Existing selectedAgents JSON semantics remain unchanged.
</schema_push_decision>

<ui_coverage_decision>
The terminal checkbox is user-facing but not a browser UI. Unit tests cover the deterministic prompt model and renewed real-TTY UAT covers actual rendering/cancellation; no UI-SPEC or browser automation applies.
</ui_coverage_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Ambient process environment → interactive prompt | Agent-owned environment variables must not influence displayed choices, implied recommendations, or installation intent. |
| User-submitted runtime IDs → adapter plan | Only the explicit submitted subset may authorize adapter files and selectedAgents persistence. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-24-01 | Spoofing / Elevation of Privilege | runtime-selection prompt | medium | mitigate | Remove environment-derived labels and prove every option is equal and unchecked. |
| T-24-02 | Tampering | CLI selection → init plan | high | mitigate | Preserve submitted AgentId values as the only adapter authority and retain subset/tree assertions. |
| T-24-03 | Repudiation | planning contract vs implementation evidence | medium | mitigate | Update active intent before code and retain the later UAT decision, focused tests, execution summary, and renewed human checkpoint. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status | Notes |
|---|---|---|---|---|
| GOAL | Only selected runtime adapters are initialized | Tasks 1-2 | COVERED | Detection removal narrows presentation metadata; explicit selection and adapter outputs remain unchanged. |
| REQ | SETUP-03 | Tasks 1-2 | COVERED | All supported subsets remain selectable interactively and by flags. |
| REQ | SETUP-04 | Task 1 | COVERED | Active requirement is revised to the newer detection-free UAT decision and implemented fully. |
| REQ | SETUP-05 | Tasks 1-2 | COVERED | Only submitted runtime adapters are written under both clean and populated environments. |
| REQ | Other Phase 1 requirement IDs | Existing Plans 01-01 through 01-23 plus full regression | COVERED | Not re-owned or changed by this gap; the full suite protects them. |
| RESEARCH | Existing runtime-selection architecture | Task 1 | COVERED | Reuses the fixed registry and Inquirer prompt; no dependency or architecture research is needed. |
| CONTEXT | D-05 superseded label clause; D-06 through D-08 | Tasks 1-2 | COVERED | Newer UAT direction is explicit; all unaffected locked behavior is preserved. |
| UAT | G-01-1 | Tasks 1-2 and renewed UAT | COVERED | Code and contracts remove detection; real terminal evidence remains human-owned. |
| STATE | FIND-NNN/PAC-NNN alignment concern | None | EXCLUDED | Separate open concern, explicitly not folded into this runtime-selection gap plan. |

