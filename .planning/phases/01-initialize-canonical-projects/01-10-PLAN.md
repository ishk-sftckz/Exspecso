---
phase: 01-initialize-canonical-projects
plan: 10
type: execute
wave: 10
depends_on: [01-09]
files_modified:
  - package.json
  - native/build.mjs
  - tests/integration/init-codex-tracer.test.ts
  - README.md
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-06, SETUP-07, ART-01, ART-02, ART-07, ART-08, ART-09]
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[GAP-PATH-05; SETUP-01/02/06] A real source-install tarball containing native source but no .node binary installs into an isolated temporary runner with lifecycle scripts enabled, builds and loads its own approved provider, and initializes the exact minimal canonical files and selected adapters from root and nested directories."
    - "[GAP-PATH-06; ART-07/08] Removing the installed provider from a separate clean installation makes its installed CLI fail explicitly before any repository byte, directory, symlink, ownership, or staging mutation; no workspace provider or unsafe fallback satisfies the test."
    - "[GAP-PATH-07; D-19] Source-install prerequisites and the actual tested platform are explicit; a missing approved compiler/header prerequisite fails without automatic downloads, new native dependencies, or a claim about untested platforms."
  artifacts:
    - path: package.json
      provides: "Approved source-install lifecycle and package files, retaining the existing dependency graph and excluding host-built native binaries"
    - path: native/build.mjs
      provides: "Package-relative local build usable from an isolated npm installation, with explicit prerequisite failures and no downloads"
    - path: tests/integration/init-codex-tracer.test.ts
      provides: "Real source tarball, isolated lifecycle-enabled installation, installed-provider load trace, root/nested CLI behavior, and unavailable-provider zero-write regressions"
    - path: README.md
      provides: "Approved source-install/compiler/header contract and exact tested platform/evidence boundary"
  key_links:
    - from: package.json
      to: native/build.mjs
      via: "The normal install lifecycle invokes the checked-in script inside the installed package"
      pattern: native/build.mjs
    - from: tests/integration/init-codex-tracer.test.ts
      to: package.json
      via: "npm pack creates an actual source tarball; npm install executes its lifecycle in an isolated runner"
      pattern: packAndRun
    - from: tests/integration/init-codex-tracer.test.ts
      to: src/filesystem/contained-fs.ts
      via: "The installed CLI loads the runner's compiled provider and exercises real init; removal produces the stable pre-mutation failure"
      pattern: contained-fs
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

This is the bounded packaging/evidence portion split from 01-09 during checker revision. Its native-provider approval remains 01-09 Task 1, before any implementation. Rejection or an unmet approved support requirement stops both plans. The historical MVP framing warning remains unchanged.

<objective>
Close GAP-PATH-05's source-install evidence gap and prove provider-unavailable behavior through the installed CLI. Preserve D-01, D-08, D-14, D-16, D-18, and D-19 while completing CR-03's package acceptance path.
Purpose: A locally built provider or a tarball listing cannot establish that users can install and safely run the package.
Output: An approved source-install lifecycle, executable isolated-install regressions in the existing tracer, and precise installation/support documentation. No new dependency, public flag, provider choice, or canonical artifact.
</objective>

<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-09-PLAN.md
@.planning/phases/01-initialize-canonical-projects/01-09-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-VERIFICATION.md
@.planning/phases/01-initialize-canonical-projects/01-GAP-COVERAGE.md
</context>

<interfaces>
The existing local packAndRun helper in init-codex-tracer.test.ts builds and packs the checkout, installs using disabled lifecycle scripts, and immediately invokes the runner's .bin/exspecso. Replace that installation fixture with the source-install contract below; all existing CLI assertions remain. tests/helpers/run-cli.ts already exposes cwd, env, and stdin and returns exitCode/stdout/stderr. It merges process.env with overrides, so removing an inherited variable requires an explicit undefined override; supplying a smaller object does not isolate the child. Keep that shared helper unchanged and sanitize subprocess environments in the tracer. execFileAsync already accepts a complete env object directly.

01-09 supplies native/contained-fs.cc, native/build.mjs, src/filesystem/contained-fs.ts, and the provider-unavailable diagnostic. Its output path is dist/native/contained-fs.node, resolved from the installed package, never the current repository. This plan changes build/install packaging and executable evidence, not containment or ownership semantics. Any failing code-level safety contract returns to 01-09 for correction, never to a weaker test.
</interfaces>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and canonical artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>Installation evidence does not change the D-05 through D-07 selection authority or D-20 artifact vocabulary. 01-09's explicit approval is the sole native/provider/build decision.</rationale>
</assumption_delta_decision>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Build the provider during an isolated source install and run root init</name>
  <precondition>The current host has the local C++ compiler and compatible Node headers required by the exact approval recorded in 01-09-SUMMARY.md; no missing prerequisite may be installed automatically.</precondition>
  <read_first>
    - package.json — existing scripts, dist package contents, exact dependency graph, and bin
    - native/build.mjs — approved local compiler/header discovery and native output from 01-09
    - tests/integration/init-codex-tracer.test.ts — packAndRun, temporary-path cleanup, existing root assertion
    - tests/helpers/run-cli.ts — existing environment merge semantics; do not modify
    - tests/helpers/git-fixture.ts — existing isolated repository fixtures; do not modify
    - src/filesystem/contained-fs.ts — installed-module-relative loader and stable unsupported-provider failure; consume unchanged
    - .planning/phases/01-initialize-canonical-projects/01-09-SUMMARY.md — exact approval, prerequisites, and implementation evidence
  </read_first>
  <files>package.json, native/build.mjs, tests/integration/init-codex-tracer.test.ts</files>
  <behavior>
    - The named source install builds provider and initializes from root regression fails before package lifecycle correction, then succeeds from an actual produced tarball installed in a fresh temporary runner outside the checkout.
    - The tarball contains compiled JavaScript, native/contained-fs.cc, and native/build.mjs, and contains no .node file. A successful lifecycle install creates dist/native/contained-fs.node inside the runner's installed exspecso package.
    - The installed CLI successfully loads exactly that installed provider and creates only the canonical foundation and selected Codex adapter with the existing D-01/D-08 assertions.
    - Neither the installed package nor the loaded provider resolves into the checkout or its node_modules; no inherited Node loader path or workspace linkage supplies a substitute.
  </behavior>
  <action>Extend the existing root tracer first, name it source install builds provider and initializes from root, and record its red result against the incomplete source-install contract. Refactor packAndRun within the same test file into reusable pack/install/run stages. Build the distribution, then use npm pack --json --pack-destination with a new temporary packing directory to create a real tarball. Validate the actual tarball entry inventory: required compiled/native source/build inputs are present and every .node entry is absent. Do not remove a checkout binary and call that proof, or copy any built provider into the runner. Create a separate fresh temporary runner outside the checkout with its own private package manifest and no node_modules. Run npm install --ignore-scripts=false --foreground-scripts --no-package-lock --no-audit --no-fund --prefix followed by the runner path and absolute tarball path, with cwd set to that runner. Capture install exit/stdout/stderr; lifecycle failure fails the test. Use a controlled child environment with Node/npm/compiler paths but no checkout node_modules/.bin, NODE_PATH, inherited NODE_OPTIONS, or inherited npm script-suppression configuration. Use empty temporary npm user/global configuration files; pass explicit undefined removals for disallowed inherited keys to runCli because it merges environments. Preserve only the approved local header/compiler settings, not an alternate provider path. Add the approved install lifecycle invoking node native/build.mjs; retain the TypeScript build and existing dependencies, include native source/build inputs, and exclude host-built .node outputs from the source tarball. Ensure native/build.mjs resolves source/output paths relative to its own installed package and uses only locally checked compiler/headers, never working-directory assumptions, devDependencies, downloaded headers/binaries, node-gyp, or a new package. Assert the package directory is not a workspace symlink and realpath of the installed bin/provider remains inside runner/node_modules/exspecso. Exercise that installed CLI through runCli using process.execPath and the installed bin's realpath. A temporary test-owned CommonJS preload passed explicitly with --require may wrap process.dlopen, forward its arguments/return unchanged, and record a successful native load outside the target repository; assert the loaded realpath equals the runner's provider. Do not mock provider exports or filesystem operations. Keep all existing root artifact/adapter assertions. Reuse one successful installation for read-only installation consumers with suite-level cleanup, while each init receives a fresh Git fixture; installations modified by negative tests must be separate. Do not retain disabled-lifecycle installation as an alternate acceptance path: no prebuilt-provider contract is approved.</action>
  <verify><automated>npm test -- --run tests/integration/init-codex-tracer.test.ts -t "source install builds provider and initializes from root"</automated></verify>
  <acceptance_criteria>Red/green evidence shows an actual tarball without a native binary becomes a working installed package only through its enabled lifecycle. The test traces successful loading of the installed native binary, runs root init with unchanged D-01/D-08 assertions, and rejects checkout or node_modules linkage as evidence. No new dependency, native download, provider choice, or approval bypass is introduced.</acceptance_criteria>
  <done>A user on the approved host can source-install the package into a clean location and initialize a repository using the provider built by that install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove nested installed-CLI behavior and unavailable-provider refusal, then document the contract</name>
  <read_first>
    - tests/integration/init-codex-tracer.test.ts — source-only tarball/install fixture and unchanged historical cases
    - native/build.mjs — package-relative build and explicit prerequisite diagnostics
    - README.md — preserve existing content if present; otherwise create package/build/safety guidance only
    - tests/helpers/run-cli.ts — reuse unchanged with sanitized overrides
    - src/filesystem/contained-fs.ts — existing provider failure boundary; do not replace it with a test bypass
    - .planning/phases/01-initialize-canonical-projects/01-CONTEXT.md — D-14/D-16/D-18/D-19
  </read_first>
  <files>native/build.mjs, tests/integration/init-codex-tracer.test.ts, README.md</files>
  <behavior>
    - Existing deep-nested and nested-Git-repository cases use the clean source installation, resolve the intended containing root, and trace the installed provider load; no canonical artifacts appear under the caller's nested directory or the wrong parent repository.
    - In a separate fresh lifecycle-enabled installation, remove its provider and invoke that installed CLI with valid init --agent codex arguments. It returns nonzero with the stable native-provider diagnostic and repair hint, emits no success guidance, and leaves a complete before/after repository byte/directory/symlink inventory identical, including lock and staging paths.
    - Point EXSPECSO_NODE_INCLUDE_DIR at a deliberately unavailable directory for another clean installation. The source build fails explicitly rather than fetching headers, compiling against a guessed location, or substituting a prebuilt/workspace provider; the repository fixture remains untouched.
    - Existing invalid-agent and no-Git-root diagnostics, additive rerun, JSON rejection, selected adapter behavior, minimal persistent tree, and D-19 recovery/race suites remain required.
  </behavior>
  <action>Expand the same tracer before adjusting remaining build behavior. Route all existing packed CLI cases through the isolated source-install fixture and retain every historical assertion; root, deep nested, and nested-repository successes must trace the installed provider load. Add source install unavailable provider and source install rejects unsupported build prerequisites regressions. For provider unavailability, allocate a separate installation from the same source tarball with normal lifecycle scripts, confirm its provider was built, then remove only that installed .node file and invoke the installed CLI against a valid fresh Git fixture with sentinel content. Snapshot the entire fixture using lstat-based traversal without following symlinks, including files, directories, symlink targets, and operational lock/staging paths, before and after invocation. Assert the stable provider diagnostic and nonzero result, no completion text or recorded native load, and exact snapshot equality. This must be the production loader's failure, not an invalid-agent shortcut, alternate CLI, source-module invocation, mocked syscall, or new safety-bypass flag. Independently force unavailable headers through EXSPECSO_NODE_INCLUDE_DIR during a fresh lifecycle-enabled installation; make the build honor an explicit header directory as authoritative and fail actionably before attempting compilation/download when invalid. Preserve compiler/header argument-array invocation and no automatic downloads. Document only the approved source-install contract in README.md: compiler/Node header requirements, EXSPECSO_NODE_INCLUDE_DIR, normal lifecycle requirement, package-local output, unsupported-provider diagnostics, exact tested Node/OS/architecture/filesystem, and D-19 limitations. Keep the dependency graph unchanged; any new npm/pip/cargo prerequisite requires a Package Legitimacy Audit and blocking human approval before it can be installed. Run the focused installed-CLI suite, then the full build/test/package gate below. Retain independent phase verification, real-TTY UAT, all five judgment acknowledgements, and the separate security audit before phase advancement.</action>
  <verify><automated>npm test -- --run tests/integration/init-codex-tracer.test.ts</automated><human-check>After the full automated gate and independent re-verification, perform real-TTY selection UAT and acknowledge the four retained plus one new judgment-tier prohibitions. Run $gsd-secure-phase 1 separately; installed-CLI tests are neither a security audit nor a TTY observation.</human-check></verify>
  <acceptance_criteria>GAP-PATH-05/06 pass through the lifecycle-built installed CLI for root/nested targeting and controlled missing-provider refusal. The refusal retains all repository bytes and directory/symlink inventory, including no operational debris; a local .node or workspace/node_modules linkage cannot satisfy success. Unsupported build prerequisites yield explicit failure without a new native dependency or download. Full regression/package results and actual tested platform are recorded; independent/human/security obligations remain pending unless separately completed.</acceptance_criteria>
  <done>Installed initialization and provider-unavailable refusal are executable regressions, and users can inspect the exact approved install/support contract without an overstated phase-completion claim.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source tarball → install lifecycle | Native compilation must use the approved checked-in code and local prerequisites. |
| Checkout/environment → temporary runner | Workspace linkage or inherited loader paths could make a broken package appear runnable. |
| Unavailable installed provider → repository mutation | The loader must fail before ownership, staging, or canonical writes. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Spoofing | installed-provider evidence | high | mitigate | Source tarball has no .node; isolated runner and sanitized environment; realpath and successful process.dlopen trace establish the provider built in the installed package. |
| T-10-02 | Tampering | missing-provider init | high | mitigate | Remove only the isolated installed provider, run the real installed CLI with valid input, and assert stable failure plus complete repository byte/tree equality. |
| T-10-03 | Denial of service | source-install prerequisites | medium | mitigate | Explicit compiler/header checks, authoritative explicit include path, failed-install regression, and documented approved support boundary. |
| T-10-SC | Tampering | native build / package installs | high | mitigate | Install only the local source tarball and unchanged approved dependency graph; native build uses checked-in C Node-API source/local compiler/headers with no downloads. Any additional npm/pip/cargo dependency stops for a Package Legitimacy Audit and blocking human approval. |
</threat_model>

## Artifacts this phase produces

| Kind | Name | Contract |
|---|---|---|
| Existing package/build files extended | package.json; native/build.mjs | Approved lifecycle-enabled package-relative source build; no packaged host .node or new dependency |
| Existing tests extended | tests/integration/init-codex-tracer.test.ts | Actual tarball/install/load/root/nested/refusal evidence and retained historical CLI assertions |
| Test fixture internals | packAndRun pack/install/run stages; temporary load probe and repository snapshots | Isolated subprocesses and real provider tracing; no public flags or persistent user state |
| Existing helper consumed unchanged | tests/helpers/run-cli.ts | Its env overrides already support removing inherited keys; no helper mutation required |
| Documentation | README.md | Approved compiler/header/install contract and measured evidence boundary |

<verification>
Run each focused task command, then run npm run build, npm test -- --run, and npm pack --dry-run as separate required final commands. The full suite includes the real tarball creation, normal lifecycle installation, provider load tracing, installed root/nested CLI executions, and provider-unavailable zero-write regression; the dry-run is an additional contents check only. Keep focused feedback within 60 seconds by reusing the unmodified successful installation and separating build/pack/install/test invocations where needed, without omitting any assertion. Record actual durations and platform. Independent phase verification, real-TTY UAT, five judgment acknowledgements, and $gsd-secure-phase 1 remain separate obligations.
</verification>

<success_criteria>
01-09's explicit approval precedes all changes. A source-only tarball installs and builds its own provider in isolation, the installed CLI loads that provider for root/nested init, and controlled provider unavailability fails before any repository mutation. Full recovery/JSON/selection/rerun/minimal-tree and package gates pass. No existing gap, historical edge, prohibition, or manual/security obligation is silently closed by planning or by a package contents check.
</success_criteria>

<output>Create .planning/phases/01-initialize-canonical-projects/01-10-SUMMARY.md with the 01-09 approval reference, red/green source-install evidence, tarball inventory, lifecycle/install output, actual installed provider path, root/nested/refusal results, full build/test/package results, measured platform/durations, and outstanding independent/UAT/security gates.</output>
