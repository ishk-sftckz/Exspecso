---
phase: 01-initialize-canonical-projects
plan: 10
type: execute
wave: 10
depends_on: [01-09]
files_modified: ["native/contained-fs.cc", "native/contained-fs-posix.cc", "native/build.mjs", "src/filesystem/contained-fs.ts", "src/filesystem/transaction.ts", "tests/unit/contained-fs.test.ts", "native/contained-fs-win.cc", "tests/integration/init-codex-tracer.test.ts", "tests/helpers/containment-fixture.ts"]
autonomous: false
requirements: ["SETUP-01", "SETUP-02", "SETUP-06", "ART-01", "ART-07"]
gap_closure: true
must_haves:
  truths:
    - "[GAP-PATH-02] The actual initializer promotion path uses opaque handles and atomic sibling replacement; deterministic tests reach the provider operation and external sentinels remain unchanged."
    - "POSIX and Windows implementations meet one contract before expansion; no pathname fallback or inference from macOS success."
    - "Instrumented installed tracer evidence is labeled separately from uninstrumented package evidence; a partial tracer is not release-ready containment."
  artifacts:
    - {"path": "native/contained-fs.cc", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 contained-fs.cc"}
    - {"path": "native/contained-fs-posix.cc", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 contained-fs-posix.cc"}
    - {"path": "native/build.mjs", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 build.mjs"}
    - {"path": "src/filesystem/contained-fs.ts", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 contained-fs.ts"}
    - {"path": "src/filesystem/transaction.ts", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 transaction.ts"}
    - {"path": "tests/unit/contained-fs.test.ts", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 contained-fs.test.ts"}
    - {"path": "native/contained-fs-win.cc", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 contained-fs-win.cc"}
    - {"path": "tests/integration/init-codex-tracer.test.ts", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 init-codex-tracer.test.ts"}
    - {"path": "tests/helpers/containment-fixture.ts", "provides": "Prove the same-package native promotion tracer on POSIX and Windows \u2014 containment-fixture.ts"}
  key_links:
    - {"from": "native/contained-fs.cc", "to": "tests/helpers/containment-fixture.ts", "via": "Implementation and evidence share the approved contract and named matrix rows", "pattern": "containment"}
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

<objective>
Prove the same-package native promotion tracer on POSIX and Windows. Preserve D-01 through D-20 and the historical MVP goal-format warning; do not rewrite the goal or count planning as verification.
Purpose: Close CR-03 within the explicitly approved platform, safety and evidence contract.
Output: The bounded artifacts and evidence below; subsequent serial plans still gate phase completion.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-REPLAN.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-09-SUMMARY.md
</context>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and canonical artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>D-05 through D-07 already resolve selection; native provider variants do not introduce canonical identities.</rationale>
</assumption_delta_decision>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Build the provider seam and POSIX vertical tracer</name>
  <read_first>native/contained-fs.cc; native/contained-fs-posix.cc; native/build.mjs; src/filesystem/contained-fs.ts; src/filesystem/transaction.ts; tests/unit/contained-fs.test.ts; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>native/contained-fs.cc, native/contained-fs-posix.cc, native/build.mjs, src/filesystem/contained-fs.ts, src/filesystem/transaction.ts, tests/unit/contained-fs.test.ts, tests/integration/init-codex-tracer.test.ts, tests/helpers/containment-fixture.ts</files>
  <precondition>01-09 explicit approval and read-only compiler/header compatibility checks pass.</precondition><reversibility rating="costly">Native/toolchain obligations require explicit approval and cannot be hidden in npm install.</reversibility>
  <action>After 01-09 approval, define opaque RootCapability/DirectoryCapability/FileCapability and openContainedFilesystem; use C Node-API version 8, no raw handle IDs or arbitrary paths after initial root acquisition. Define native/build.mjs arguments --variant release or test, --target the approved tuple, and --out a build directory; run the selected native build before each green provider/tracer test. It uses approved compiler/header pins and argument arrays, builds a release variant plus separately labeled instrumented variant, and has no install lifecycle. Component operations reject dot/dotdot, separators, NUL and wrong types. Implement exclusive temp creation, checked write loops, sync, bound read/stat/list, sibling atomic replacement, unlink and empty-directory removal. POSIX walks one component with no-follow opens; Linux openat2 constraints and supported fallback are explicitly tested. Route one real commitTransaction promotion through this seam while retaining hashes and journal evidence. Add provider tests and a deterministic regression before replacing the vulnerable copy behavior. Existing readers/staging are not yet fully migrated; do not expose the partial implementation as a completed secure release. Before any installed tracer or 01-12 read migration, implement the minimal fixed loader and host package layout in this task. native/build.mjs writes the approved host release or test binary into its own staging tree at dist/native/TARGET/contained-fs.node and generates dist/native/manifest.json with exact tuple, package version, build commit, Node-API version, byte length and SHA-256. The TypeScript loader anchors the package root to its own module location (src/filesystem and compiled dist/filesystem are both two levels below that root), then reads only the fixed dist/native/manifest.json and matching in-package binary; source tests first build those same fixed package artifacts. It never uses cwd or a caller-supplied root to locate native code. The loader checks its metadata/hash before load, and refuses unknown or missing entries before writes. package.json already ships dist, so tests build TypeScript into the same staging tree and npm pack that tree with the current package metadata; no source-lifecycle hook or external development loader is needed. Distinguish release and test staging/package roots so one variant cannot leak into another. The minimal host package is only a tracer artifact, never a supported all-platform candidate. Plan 16 later expands this established schema/loader to all approved target binaries and strengthens isolated-install negatives; it does not create a dependency that 10 or 12 needs earlier. Record which operations remain pathname-based for 12-15. Extend tests/integration/init-codex-tracer.test.ts and create tests/helpers/containment-fixture.ts in this task to drive the first POSIX installed CLI path; Task 3 expands its installed evidence. The first tracer must already reach the real CLI, transaction and provider before expansion.</action>
  <verify><automated>npm test -- --run tests/unit/contained-fs.test.ts tests/integration/init-codex-tracer.test.ts</automated></verify>
  <acceptance_criteria>Provider cases NP-01/02 pass on the local approved POSIX environment and one real promotion is bound; missing/incompatible provider fails before mutation.</acceptance_criteria>
  <done>Provider cases NP-01/02 pass on the local approved POSIX environment and one real promotion is bound; missing/incompatible provider fails before mutation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement and prove Windows handle-relative parity</name>
  <read_first>native/contained-fs-win.cc; native/contained-fs.cc; native/build.mjs; src/filesystem/contained-fs.ts; tests/unit/contained-fs.test.ts; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>native/contained-fs-win.cc, native/contained-fs.cc, native/build.mjs, src/filesystem/contained-fs.ts, tests/unit/contained-fs.test.ts</files>

  <action>Implement Windows user-mode handle-relative component acquisition and replacement using the researched NtCreateFile/RootDirectory and handle-based rename facilities, not Win32 pathname reconstruction. Verify exact supported API structures/flags with the pinned SDK before coding. Reject reparse points, junctions, device/ADS forms and wrong kinds; define sharing/delete flags and stable error mapping. Tests must exercise actual Windows native operations and replacement/open-handle sharing cases. An absent native Windows runner, unsupported primitive or failed W-01/W-02 feasibility stops the plan; do not emulate a passing Windows result or add an unsafe fallback. Close native resources on every failure and JavaScript exception.</action>
  <verify><automated>npm test -- --run tests/unit/contained-fs.test.ts</automated></verify>
  <acceptance_criteria>NP-01/02 and Windows-specific cases pass on an approved native Windows runner with recorded OS/SDK/Node/hash evidence; local POSIX tests do not satisfy this task.</acceptance_criteria>
  <done>NP-01/02 and Windows-specific cases pass on an approved native Windows runner with recorded OS/SDK/Node/hash evidence; local POSIX tests do not satisfy this task.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Exercise the real installed initializer tracer</name>
  <read_first>tests/integration/init-codex-tracer.test.ts; tests/helpers/containment-fixture.ts; native/build.mjs; src/filesystem/transaction.ts; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>tests/integration/init-codex-tracer.test.ts, tests/helpers/containment-fixture.ts, native/build.mjs, native/contained-fs-posix.cc, native/contained-fs-win.cc, src/filesystem/transaction.ts</files>

  <action>Reuse Git fixtures and extend the existing packed tracer. Construct a real isolated tarball/install containing the host provider, invoke its actual CLI and record the loaded provider hash/path. Keep release and instrumented packages distinct. The instrumented build may expose a fixed operation-ID synchronization channel for an independent controller, but never an arbitrary output/provider path or alternate production state machine. On Windows, the test-only addon owns its one-instance local named-pipe endpoint and conditional system-library linkage; it accepts only the fixed activation tuple, validates the controller process, and removes all CRT/stdio-handle discovery. Prove the existing post-validation leaf/parent/ancestor regression red before correction, then green for the bound promotion path; include an ordinary successful replacement. Confirm barrier-to-operation placement by native call trace/code inspection and reached IDs. Run uninstrumented tracer E2E separately and document Mode B limits. Do not pretend the early host-only tarball is the final all-target release package.</action>
  <verify><automated>npm test -- --run tests/integration/init-codex-tracer.test.ts -t "contained promotion tracer"</automated></verify>
  <acceptance_criteria>TR-01/02 have actual CLI/install/provider evidence on one POSIX and one Windows environment; deterministic evidence is accurately labeled and earlier packed cases remain intact.</acceptance_criteria>
  <done>TR-01/02 have actual CLI/install/provider evidence on one POSIX and one Windows environment; deterministic evidence is accurately labeled and earlier packed cases remain intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Repository names, native handles, package contents and platform evidence must not acquire authority implicitly.
## STRIDE Threat Register — ASVS Level 1; block high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Tampering | Native names and handles | high | mitigate | No-follow single-component operations and opaque lifetime tests |
| T-10-02 | Tampering | Windows parity | high | mitigate | Native runner evidence and explicit API feasibility stop |
| T-10-03 | Tampering | Evidence substitution | high | mitigate | Distinct instrumented/release packages and actual installed provider trace |
</threat_model>

## Artifacts this phase produces

- native/contained-fs.cc
- native/contained-fs-posix.cc
- native/build.mjs
- src/filesystem/contained-fs.ts
- src/filesystem/transaction.ts
- tests/unit/contained-fs.test.ts
- native/contained-fs-win.cc
- tests/integration/init-codex-tracer.test.ts
- tests/helpers/containment-fixture.ts

New symbols, protocols and commands are explicitly named in the task actions; no hidden canonical store is introduced.

<verification>Run each focused command without watch mode, build and existing regression checks after implementation tasks. New regression cases must fail against the vulnerable behavior before correction; tests of newly introduced infrastructure must fail before that infrastructure exists. Record reached case IDs, actual environments, command exit status and logs. Missing or skipped required evidence is not a pass. Keep feedback shards bounded; do not omit cases to meet a time target.</verification>
<success_criteria>All task acceptance criteria and actual evidence pass within the approved 01-09 decision. A human rejection or missing required environment stops the dependency chain.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-10-SUMMARY.md with approval history, actual evidence, and remaining gates.</output>

## Scope boundary

This is one vertical promotion tracer split into three sequential tasks: POSIX with the minimum installed host package, Windows parity, and installed evidence expansion. The small native facade, two OS files, TypeScript wrapper, build script and shared fixture are one integration surface, not separate product features. Do not implement release aggregation, all-environment claims, artifact-read migration, new journal policy or complete cleanup here. The file count reflects the vertical boundary; scope is limited to existing filesystem primitives and one actual initializer promotion path. Plan 12 consumes the already-created loader; it must not rebuild packaging.
