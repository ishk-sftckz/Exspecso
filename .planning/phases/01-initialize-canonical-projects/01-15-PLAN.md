---
phase: 01-initialize-canonical-projects
plan: 15
type: execute
wave: 16
depends_on: [01-14]
files_modified: ["src/filesystem/transaction.ts", "src/filesystem/recovery.ts", "src/filesystem/ownership.ts", "src/filesystem/journal.ts", "tests/integration/transaction-recovery.test.ts", "native/build.mjs", "tests/unit/contained-fs.test.ts", ".github/workflows/containment.yml"]
autonomous: true
requirements: ["SETUP-06", "ART-01", "ART-02", "ART-07", "ART-08", "ART-09"]
gap_closure: true
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "[GAP-PATH-03] Cleanup and recovery remove only identified entries using bound parents, never recursive pathname deletion or unknown evidence."
    - "[GAP-PATH-04] D-18/D-19 recovery and cleanup remain restartable after each declared process fault; mixed output is never accepted."
    - "Native handle misuse, malformed names, resource leaks and write/sync failures have bounded regression and sanitizer evidence."
    - "Native diagnostic workflows preserve every support-matrix row and named Node lane, including ENV-MA25 and exact Node 25.2.1; no fixed eight-row assumption returns."
  artifacts:
    - {"path": "src/filesystem/transaction.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 transaction.ts"}
    - {"path": "src/filesystem/recovery.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 recovery.ts"}
    - {"path": "src/filesystem/ownership.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 ownership.ts"}
    - {"path": "src/filesystem/journal.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 journal.ts"}
    - {"path": "tests/integration/transaction-recovery.test.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 transaction-recovery.test.ts"}
    - {"path": "native/contained-fs.cc", "provides": "Finish conservative cleanup and native resource safety \u2014 contained-fs.cc"}
    - {"path": "native/contained-fs-posix.cc", "provides": "Finish conservative cleanup and native resource safety \u2014 contained-fs-posix.cc"}
    - {"path": "native/contained-fs-win.cc", "provides": "Finish conservative cleanup and native resource safety \u2014 contained-fs-win.cc"}
    - {"path": "native/build.mjs", "provides": "Finish conservative cleanup and native resource safety \u2014 build.mjs"}
    - {"path": "tests/unit/contained-fs.test.ts", "provides": "Finish conservative cleanup and native resource safety \u2014 contained-fs.test.ts"}
    - {"path": ".github/workflows/containment.yml", "provides": "Finish conservative cleanup and native resource safety \u2014 containment.yml"}
  key_links:
    - {"from": "src/filesystem/transaction.ts", "to": ".github/workflows/containment.yml", "via": "Implementation and evidence share the approved contract and named matrix rows", "pattern": "containment"}
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

<objective>
Finish conservative cleanup and native resource safety. Preserve D-01 through D-20 and the historical MVP goal-format warning; do not rewrite the goal or count planning as verification.
Purpose: Close CR-03 within the explicitly approved platform, safety and evidence contract.
Output: The bounded artifacts and evidence below; subsequent serial plans still gate phase completion.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-REPLAN.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-14-SUMMARY.md
</context>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and canonical artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>D-05 through D-07 already resolve selection; native provider variants do not introduce canonical identities.</rationale>
</assumption_delta_decision>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make cleanup restartable without destroying needed evidence</name>
  <read_first>src/filesystem/transaction.ts; src/filesystem/recovery.ts; src/filesystem/ownership.ts; src/filesystem/journal.ts; tests/integration/transaction-recovery.test.ts; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>src/filesystem/transaction.ts, src/filesystem/recovery.ts, src/filesystem/ownership.ts, src/filesystem/journal.ts, tests/integration/transaction-recovery.test.ts</files>

  <action>Extend CL-01/02 and recovery fault cases before migration. Enumerate known transaction files through bound directories and remove only positively identified entries. Do not use recursive rm or follow a reconstructed pathname. Preserve the last recovery authority until a validated prior/new artifact set and terminal cleanup state are durable within D-19. If terminal journal publication and file removal cannot be ordered safely with the proposed format, stop and revise the recorded operational protocol rather than guessing. Interrupted cleanup must restart without requiring already-deleted backups to prove a settled valid set. Unknown entries, identity/token drift or altered evidence stop and preserve remaining data. Release only the owned UUID lease. Include partial stage, legacy journal, second live owner and cleanup restart cases.</action>
  <verify><automated>npm test -- --run tests/integration/transaction-recovery.test.ts tests/integration/minimal-artifacts.test.ts</automated></verify>
  <acceptance_criteria>CL-01/02 and every named cleanup fault either complete validated cleanup or preserve ambiguous evidence; no unknown/live-owned entry is removed.</acceptance_criteria>
  <done>CL-01/02 and every named cleanup fault either complete validated cleanup or preserve ambiguous evidence; no unknown/live-owned entry is removed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Verify native safety and platform fault behavior</name>
  <read_first>native/contained-fs.cc; native/contained-fs-posix.cc; native/contained-fs-win.cc; native/build.mjs; native/support-matrix.json; tests/unit/contained-fs.test.ts; .github/workflows/containment.yml; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; 01-20-SUMMARY.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>native/build.mjs, tests/unit/contained-fs.test.ts, .github/workflows/containment.yml</files>

  <action>This task owns diagnostic build configuration, tests and CI only; the provider API and support-row matcher are predecessor outputs. A newly exposed native defect blocks this plan and requires a bounded correction to the owning provider task before rerunning this evidence, not an unscoped redesign here. Extend NP-03/04 and EF-01/02. Exercise use after close, wrong handle kind/parent, repeated close, exception/partial-create cleanup, Unicode and length boundaries, invalid buffers, concurrent close policy and resource exhaustion. Define whether async operations serialize handle lifetime; test that chosen rule. Add maintained sanitizer build flags supported by each pinned toolchain and bounded malformed-input/fuzz cases with recorded seed/count. Derive diagnostic jobs from `native/support-matrix.json`; retain all eight prior rows plus `ENV-MA25` and all ten named Node lanes, with the local row executed through its recorded external evidence path rather than a fictional hosted runner. Distinguish synthetic short-write/sync/full-disk injection from real read-only/share-denial/filesystem observations. If a sanitizer is unavailable on a row, require the exact alternate diagnostic tool and explicit coverage disposition; never quietly skip. Run directory-handle leak checks in subprocesses so the harness does not conceal leaks. Sanitizer/instrumented builds do not replace release E2E.</action>
  <verify><automated>npm test -- --run tests/unit/contained-fs.test.ts</automated></verify>
  <acceptance_criteria>All native/fault cases have per-row outcomes across the matrix-derived nine-plus rows and ten named Node lanes; required diagnostics pass, ENV-MA25 remains required, and no regression assertion is weakened.</acceptance_criteria>
  <done>Native diagnostics preserve the expanded support contract and produce explicit per-row outcomes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Repository names, native handles, package contents and platform evidence must not acquire authority implicitly.
## STRIDE Threat Register — ASVS Level 1; block high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-15-01 | Tampering | Cleanup authority | high | mitigate | Identified nonrecursive bound removal and restart faults |
| T-15-02 | Tampering | Memory/resource lifetime | high | mitigate | Opaque-handle misuse cases and pinned sanitizer diagnostics |
</threat_model>

## Artifacts this phase produces

- src/filesystem/transaction.ts
- src/filesystem/recovery.ts
- src/filesystem/ownership.ts
- src/filesystem/journal.ts
- tests/integration/transaction-recovery.test.ts
- native/build.mjs
- tests/unit/contained-fs.test.ts
- .github/workflows/containment.yml

New symbols, protocols and commands are explicitly named in the task actions; no hidden canonical store is introduced.

<verification>Run each focused command without watch mode, build and existing regression checks after implementation tasks. New regression cases must fail against the vulnerable behavior before correction; tests of newly introduced infrastructure must fail before that infrastructure exists. Record reached case IDs, actual environments, command exit status and logs. Missing or skipped required evidence is not a pass. Keep feedback shards bounded; do not omit cases to meet a time target.</verification>
<success_criteria>All task acceptance criteria and actual evidence pass within the approved 01-09 decision. A human rejection or missing required environment stops the dependency chain.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-15-SUMMARY.md with approval history, actual evidence, and remaining gates.</output>

## Scope boundary

Task 1 changes only transaction cleanup/recovery/ownership policy and its tests. Task 2 changes only native diagnostic build flags, native tests and existing CI jobs; it does not refactor the provider implementation. Separate red/green evidence is required for each task, and a provider defect stops completion until corrected in its owning scope.
