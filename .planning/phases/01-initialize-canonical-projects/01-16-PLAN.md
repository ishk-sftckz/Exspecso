---
phase: 01-initialize-canonical-projects
plan: 16
type: execute
wave: 18
depends_on: [01-15]
files_modified: ["scripts/assemble-containment-package.mjs", "tests/unit/contained-fs.test.ts", "package.json", "tests/integration/init-codex-tracer.test.ts", "tests/helpers/containment-fixture.ts"]
autonomous: true
requirements: ["SETUP-01", "SETUP-02", "SETUP-06", "ART-01", "ART-02", "ART-07"]
gap_closure: true
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "[GAP-PATH-05] The actual release tarball contains one row-qualified approved prebuilt for every support row, including both macOS arm64 rows, and installs with scripts disabled, without compiler/headers or provider acquisition."
    - "[GAP-PATH-06] Missing, corrupt or incompatible included providers yield actionable errors before repository mutation."
    - "Installed-provider paths/hashes prove no checkout, NODE_PATH, NODE_OPTIONS or workspace linkage supplies the native code."
    - "Package engine, runtime loader and assembled manifest retain the ten named Node lanes with exact Node 25.2.1 support while unlisted odd/future versions fail closed."
  artifacts:
    - {"path": "native/build.mjs", "provides": "Ship all prebuilts inside one isolated installable package \u2014 build.mjs"}
    - {"path": "native/support-matrix.json", "provides": "Ship all prebuilts inside one isolated installable package \u2014 support-matrix.json"}
    - {"path": "src/filesystem/contained-fs.ts", "provides": "Ship all prebuilts inside one isolated installable package \u2014 contained-fs.ts"}
    - {"path": "scripts/assemble-containment-package.mjs", "provides": "Ship all prebuilts inside one isolated installable package \u2014 assemble-containment-package.mjs"}
    - {"path": "tests/unit/contained-fs.test.ts", "provides": "Ship all prebuilts inside one isolated installable package \u2014 contained-fs.test.ts"}
    - {"path": "package.json", "provides": "Ship all prebuilts inside one isolated installable package \u2014 package.json"}
    - {"path": "tests/integration/init-codex-tracer.test.ts", "provides": "Ship all prebuilts inside one isolated installable package \u2014 init-codex-tracer.test.ts"}
    - {"path": "tests/helpers/containment-fixture.ts", "provides": "Ship all prebuilts inside one isolated installable package \u2014 containment-fixture.ts"}
  key_links:
    - {"from": "native/build.mjs", "to": "tests/helpers/containment-fixture.ts", "via": "Implementation and evidence share the approved contract and named matrix rows", "pattern": "containment"}
---

## Approved Phase Goal

Users can initialize an Exspecso project that has only the selected runtime adapters and minimal, durable canonical artifact foundations.

<objective>
Ship all prebuilts inside one isolated installable package. Preserve D-01 through D-20 and the historical MVP goal-format warning; do not rewrite the goal or count planning as verification.
Purpose: Close CR-03 within the explicitly approved platform, safety and evidence contract.
Output: The bounded artifacts and evidence below; subsequent serial plans still gate phase completion.
</objective>
<execution_context>
@/Users/ishk.sftckz/.codex/gsd-core/workflows/execute-plan.md
@/Users/ishk.sftckz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-initialize-canonical-projects/01-CONTEXT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-REPLAN.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-RESEARCH.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-SUPPORT.md
@.planning/phases/01-initialize-canonical-projects/01-CONTAINMENT-TEST-MATRIX.md
@.planning/phases/01-initialize-canonical-projects/01-20-SUMMARY.md
@.planning/phases/01-initialize-canonical-projects/01-15-SUMMARY.md
</context>

<assumption_delta_decision>
<generalized_primary_noun>explicit runtime selection and canonical artifact identity</generalized_primary_noun>
<decision>no-change</decision>
<rationale>D-05 through D-07 already resolve selection; native provider variants do not introduce canonical identities.</rationale>
</assumption_delta_decision>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Assemble the exact support-row prebuilt manifest</name>
  <read_first>native/build.mjs; native/support-matrix.json; src/filesystem/support-matrix.ts; src/filesystem/contained-fs.ts; scripts/assemble-containment-package.mjs; tests/unit/contained-fs.test.ts; package.json; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; 01-20-SUMMARY.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>scripts/assemble-containment-package.mjs, tests/unit/contained-fs.test.ts, package.json</files>

  <action>Consume the support-row manifest schema and loader completed in 01-19/20; do not recreate a target-only support table. `scripts/assemble-containment-package.mjs` must derive required inputs from `native/support-matrix.json`, accept exactly one approved release artifact per row, and allow multiple rows to share a native target while retaining distinct `supportRowId` paths. Assemble at least the eight prior rows plus `ENV-MA25`; reject missing, duplicate, unexpected, wrong-row, wrong-source/toolchain, wrong-commit, mismatched provider hash, or mixed-variant inputs. Retain package version, build commit, target, support-row ID, Node-API 8, byte length and SHA-256 in `dist/native/manifest.json`, and include the runtime support-matrix copy used by the loader. Keep `package.json#engines.node` equal to `^20.19.0 || ^22.13.0 || ^24.0.0 || 25.2.1 || ^26.0.0`. Do not accept a provider path from environment or caller. All provider resources stay in one npm tarball; no binding.gyp, install/preinstall/postinstall build, source compilation, provider download, or test hook. Preserve `private: true`. A manifest detects accidental/partial tampering, while verified build/source provenance supplies trust. Add PK-01/02 tests first.</action>
  <verify><automated>npm test -- --run tests/unit/contained-fs.test.ts -t "manifest|provider"</automated></verify>
  <acceptance_criteria>Every support row has one row-qualified release entry, including both darwin-arm64 rows; all trusted hashes and source/toolchain bindings match, the ten-lane engine is retained, and the package has no external fallback or test hook.</acceptance_criteria>
  <done>One private tarball contains the complete row-qualified prebuilt set and matching support policy.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove isolated installation and negative package behavior</name>
  <read_first>tests/integration/init-codex-tracer.test.ts; tests/helpers/containment-fixture.ts; scripts/assemble-containment-package.mjs; 01-CONTAINMENT-SUPPORT.md; 01-CONTAINMENT-TEST-MATRIX.md; current existing suites/fixtures and predecessor summary. New files are absent until this task creates them.</read_first>
  <files>tests/integration/init-codex-tracer.test.ts, tests/helpers/containment-fixture.ts, scripts/assemble-containment-package.mjs</files>

  <action>Extend the existing installed tracer with PK-03/04. Produce a real release tarball, prefetch the locked ordinary dependency set into a dedicated cache, then install outside the checkout using npm install --offline --ignore-scripts with fresh npm user configuration and no inherited loader/module paths. The offline claim assumes that named dependency cache exists; never imply uncached dependencies can be fetched offline. Make compiler/header tools unavailable to that install/test process. Invoke the installed CLI from root and nested repositories; record that the loaded provider belongs to the installation and manifest hash. Separate negative installs remove/corrupt/swap a provider or manifest field and assert actionable diagnostics and exact repository tree/bytes equality, including no lock/staging debris. Preserve successful original install for positive cases. Do not treat a contents-only pack dry run as installation evidence.</action>
  <verify><automated>npm test -- --run tests/integration/init-codex-tracer.test.ts -t "prebuilt install|provider unavailable"</automated></verify>
  <acceptance_criteria>The actual tarball installs and executes with scripts disabled and no provider network/build dependency; destructive negative tests prove zero repository writes.</acceptance_criteria>
  <done>The actual tarball installs and executes with scripts disabled and no provider network/build dependency; destructive negative tests prove zero repository writes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Repository names, native handles, package contents and platform evidence must not acquire authority implicitly.
## STRIDE Threat Register — ASVS Level 1; block high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-01 | Tampering | Binary supply chain | high | mitigate | Fixed manifest selection, provenance-bound assembly and private package |
| T-16-02 | Tampering | Workspace leakage | high | mitigate | Isolated offline cached install, sanitized environment and loaded-provider trace |
</threat_model>

## Artifacts this phase produces

- native/build.mjs
- native/support-matrix.json
- src/filesystem/contained-fs.ts
- scripts/assemble-containment-package.mjs
- tests/unit/contained-fs.test.ts
- package.json
- tests/integration/init-codex-tracer.test.ts
- tests/helpers/containment-fixture.ts

New symbols, protocols and commands are explicitly named in the task actions; no hidden canonical store is introduced.

<verification>Run each focused command without watch mode, build and existing regression checks after implementation tasks. New regression cases must fail against the vulnerable behavior before correction; tests of newly introduced infrastructure must fail before that infrastructure exists. Record reached case IDs, actual environments, command exit status and logs. Missing or skipped required evidence is not a pass. Keep feedback shards bounded; do not omit cases to meet a time target.</verification>
<success_criteria>All task acceptance criteria and actual evidence pass within the approved 01-09 decision. A human rejection or missing required environment stops the dependency chain.</success_criteria>
<output>Create .planning/phases/01-initialize-canonical-projects/01-16-SUMMARY.md with approval history, actual evidence, and remaining gates.</output>
